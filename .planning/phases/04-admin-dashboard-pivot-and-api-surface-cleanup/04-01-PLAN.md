---
phase: 04-admin-dashboard-pivot-and-api-surface-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/routers/admin_sessions.py
  - backend/main.py
  - backend/tests/test_admin_sessions.py
autonomous: true
requirements: [ADM-02]
tags: [backend, fastapi, admin-api, sessions]

must_haves:
  truths:
    - "Authenticated admin can GET /api/admin/sessions and receive every profile row with vendor_type, vendor_connection_id, state, attach counts, last_launched_at, uptime_seconds"
    - "Stopped profiles appear with state stopped and zero attach counts"
    - "Unauthenticated request to /api/admin/sessions returns 401"
  artifacts:
    - path: "backend/routers/admin_sessions.py"
      provides: "GET /api/admin/sessions admin ops list"
      exports: ["router", "list_admin_sessions"]
    - path: "backend/models.py"
      provides: "AdminSessionListItem response model"
      contains: "class AdminSessionListItem"
  key_links:
    - from: "backend/routers/admin_sessions.py"
      to: "session_manager.status_envelope"
      via: "Depends(get_session_manager) per profile_id"
      pattern: "status_envelope\\("
    - from: "backend/main.py"
      to: "admin_sessions.router"
      via: "app.include_router"
      pattern: "admin_sessions"
---

<objective>
Add the admin-authenticated sessions ops list API that merges SQLite profile rows with live warm-pool telemetry.

Purpose: ADM-02 requires all profiles, not only `browser_mgr.running`. The dashboard must not call machine `/sessions` (API key).
Output: `AdminSessionListItem` model, `backend/routers/admin_sessions.py`, mounted at `/api/admin/sessions`, integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-CONTEXT.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-RESEARCH.md
@backend/session_manager.py
@backend/routers/sessions.py
@backend/database.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminSessionListItem model + list handler</name>
  <files>backend/models.py, backend/routers/admin_sessions.py</files>
  <action>
Add `AdminSessionListItem` to `backend/models.py` per 04-RESEARCH.md shape (`profile_id`, `name`, `vendor_type`, `vendor_connection_id`, `state`, `cdp_attach_count`, `viewer_attach_count`, `idle_expires_at`, `last_launched_at`, `uptime_seconds`, `clipboard_sync`).

Create `backend/routers/admin_sessions.py`:
- `router = APIRouter(prefix="/api/admin/sessions", tags=["admin-sessions"])`
- `GET ""` handler:
  1. `rows = db.list_profiles()` (all profiles)
  2. For each row, `env = sm.status_envelope(row["id"])`
  3. Compute `uptime_seconds`: if `env.state in ("running", "idle")` and `env.last_launched_at` parseable, `int((now_utc - launched).total_seconds())`, else `None`
  4. Append `AdminSessionListItem(...)`
  5. Sort: `last_launched_at` desc (None last), tie-break `vendor_type` asc
- Use `Depends(get_session_manager)` factory matching `sessions.py` pattern
- Do NOT add `require_api_key` — route must stay on admin auth surface only

Mount router in `main.py` with other routers (after app.state session_manager exists). Do not modify viewer or machine routes.
  </action>
  <verify>
cd backend && python -c "from backend.models import AdminSessionListItem; from backend.routers.admin_sessions import router"
  </verify>
  <done>Import succeeds; router defines GET on prefix /api/admin/sessions</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for admin sessions list</name>
  <files>backend/tests/test_admin_sessions.py</files>
  <action>
Create `backend/tests/test_admin_sessions.py`:

1. **Auth gate:** `GET /api/admin/sessions` without admin cookie → 401 (use existing `client_no_auth` / `client_auth` fixtures from `test_auth.py` pattern).

2. **Empty DB:** authenticated → `[]`.

3. **Merged shape:** Seed profile via `db.create_profile` or upsert helper with `vendor_type` / `vendor_connection_id` (use existing test DB fixtures). Authenticated GET → one item with correct vendor fields, `state == "stopped"`, counts 0.

4. **Running profile:** With `mock_browser_manager` / session manager test pattern from `test_sessions_router.py`, register a running profile and assert `state` in `("running", "idle")`, `cdp_attach_count` or `viewer_attach_count` ≥ 0 when wired, `uptime_seconds` is not None when running.

Avoid testing machine `/sessions` in this file — admin route only.
  </action>
  <verify>cd backend && pytest tests/test_admin_sessions.py -q</verify>
  <done>All new tests pass; auth and envelope merge covered</done>
</task>

</tasks>

<verification>
- `pytest tests/test_admin_sessions.py -q` green
- `grep -r "require_api_key" backend/routers/admin_sessions.py` returns no matches
</verification>

<success_criteria>
- Admin sessions endpoint returns full profile inventory with warm-pool fields
- Machine auth surface untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-01-SUMMARY.md`
</output>
