---
phase: 04-admin-dashboard-pivot-and-api-surface-cleanup
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/hooks/useSessions.ts
  - frontend/src/hooks/useSessions.test.ts
  - frontend/src/components/SessionList.tsx
autonomous: true
requirements: [ADM-02]
tags: [frontend, typescript, react, sessions]

must_haves:
  truths:
    - "api.listAdminSessions fetches GET /api/admin/sessions with cookie auth"
    - "useSessions polls every 3s and exposes sessions, loading, error, refresh"
    - "SessionList renders vendor_type, vendor_connection_id, state, attach counts, uptime, last_launched_at"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "AdminSessionListItem type + listAdminSessions"
      contains: "listAdminSessions"
    - path: "frontend/src/hooks/useSessions.ts"
      provides: "polling hook for admin sessions"
      contains: "useSessions"
    - path: "frontend/src/components/SessionList.tsx"
      provides: "ops table UI"
      min_lines: 80
      contains: "SessionList"
  key_links:
    - from: "frontend/src/hooks/useSessions.ts"
      to: "/api/admin/sessions"
      via: "api.listAdminSessions"
      pattern: "listAdminSessions"
    - from: "frontend/src/components/SessionList.tsx"
      to: "useSessions data"
      via: "props.sessions"
      pattern: "vendor_type"
---

<objective>
Land the typed frontend data layer and Sessions ops table component consumed by the App pivot in Plan 04-03.

Purpose: Isolate React work from App.tsx routing so types and UI can be tested independently.
Output: `AdminSessionListItem`, `useSessions`, `SessionList.tsx`, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-CONTEXT.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-RESEARCH.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-01-SUMMARY.md
@frontend/src/lib/api.ts
@frontend/src/hooks/useTemplates.ts
@frontend/src/components/TemplateList.tsx
@frontend/src/components/ProfileList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API types + useSessions hook</name>
  <files>frontend/src/lib/api.ts, frontend/src/hooks/useSessions.ts, frontend/src/lib/api.test.ts, frontend/src/hooks/useSessions.test.ts</files>
  <action>
In `api.ts`:
- Add `AdminSessionListItem` interface matching backend model fields
- Add `listAdminSessions: () => request<AdminSessionListItem[]>("/api/admin/sessions")`
- Do NOT remove legacy profile methods yet (Plan 04-04)

Create `useSessions.ts` mirroring `useTemplates.ts`:
- State: `sessions`, `loading`, `error`
- `refresh()` calls `api.listAdminSessions()`
- `useEffect`: initial refresh + `setInterval(refresh, 3000)`
- `loading` true only until first successful fetch

Add tests:
- `api.test.ts`: mock fetch, assert path `/api/admin/sessions`
- `useSessions.test.ts`: vi.mock api, assert polling calls refresh (optional: use fake timers)

Add `formatUptime(seconds: number | null): string` helper in `SessionList.tsx` or `lib/format.ts` (prefer colocated in SessionList if &lt;15 lines).
  </action>
  <verify>cd frontend && npm test -- --run src/lib/api.test.ts src/hooks/useSessions.test.ts 2>&1 | tail -5</verify>
  <done>Tests pass; types exported</done>
</task>

<task type="auto">
  <name>Task 2: SessionList component</name>
  <files>frontend/src/components/SessionList.tsx</files>
  <action>
Create `SessionList.tsx` (table layout like `TemplateList.tsx`):

**Props:** `sessions: AdminSessionListItem[]`, `selectedId: string | null`, `onSelect: (id: string) => void`, `loading?: boolean`

**Columns:** vendor_type, vendor_connection_id, state (with `StatusIndicator` — map `idle` to same visual as running or add idle variant), CDP count, viewer count, uptime (`formatUptime`), last_launched_at (relative or ISO short)

**Header:** search filter across vendor_type + vendor_connection_id + name; show count of `state === "running"` or running+idle

**Row click:** calls `onSelect(profile_id)`

**No** “New session” / “New profile” button (ADM-04)

**Accessibility:** table headers, truncate long connection IDs with `title` tooltip

Do not import `App.tsx` yet.
  </action>
  <verify>cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | tail -3</verify>
  <done>Typecheck clean; component exports SessionList</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --skipLibCheck` in frontend/
- `npm test -- --run` for new test files passes
</verification>

<success_criteria>
- SessionList displays all fields required by ADM-02
- Hook polls admin endpoint only
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-02-SUMMARY.md`
</output>
