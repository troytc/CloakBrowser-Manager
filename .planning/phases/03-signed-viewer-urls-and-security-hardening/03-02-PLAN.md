---
id: 03-02
phase: 03-signed-viewer-urls-and-security-hardening
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/security_csp.py
  - backend/viewer_auth.py
  - backend/main.py
  - backend/routers/profiles.py
  - backend/tests/test_security_hardening.py
requirements:
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-07
autonomous: true
must_haves:
  truths:
    - "GET /profiles/{id}/clipboard with X-API-Key only returns 403"
    - "GET /profiles/{id}/clipboard with valid X-Viewer-Token returns 200 when profile running"
    - "Admin /api/* JSON responses include Content-Security-Policy frame-ancestors 'none'"
    - "auth_token cookie remains HttpOnly and SameSite=strict on login"
    - "Machine /sessions and /profiles routes do NOT get frame-ancestors none (admin /api only)"
  artifacts:
    - path: "backend/security_csp.py"
      provides: "CSP header helpers"
    - path: "backend/viewer_auth.py"
      provides: "require_viewer_token dependency"
    - path: "backend/routers/profiles.py"
      contains: "clipboard"
  key_links:
    - from: "backend/routers/profiles.py"
      to: "backend/viewer_auth.py"
      via: "Depends(require_viewer_token)"
      pattern: "require_viewer_token"
    - from: "backend/main.py"
      to: "backend/security_csp.py"
      via: "middleware or response hook on /api/"
      pattern: "frame-ancestors 'none'"
---

<objective>
Harden security surfaces that do not require the viewer WS route: CSP headers, clipboard viewer-scoping on machine API, and verification that admin cookie flags already meet SEC-03.

Purpose: SEC-02/04/07 can land in parallel with viewer_tokens (Plan 03-01).

Output: CSP middleware/helpers, machine clipboard GET with viewer token, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-RESEARCH.md
@CLAUDE.md
@backend/main.py
@backend/routers/profiles.py
@backend/auth_api_key.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSP helpers and admin /api/* middleware</name>
  <files>backend/security_csp.py, backend/main.py</files>
  <read_first>
    - backend/main.py (AuthMiddleware, app construction, include_router order)
    - .planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    Create `backend/security_csp.py` with:
    - `viewer_frame_ancestors_header() -> str` reading `MAIN_APP_ORIGIN` from env; if blank in non-DEV_MODE, log warning or raise at import time when viewer routes register (planner: raise in helper used by viewer router in 03-03 if empty in prod)
    - `admin_frame_ancestors_none() -> dict[str, str]` returning `{"Content-Security-Policy": "frame-ancestors 'none'"}`

    Add a small Starlette middleware (or `@app.middleware("http")`) that applies `admin_frame_ancestors_none()` only when `request.url.path.startswith("/api/")` and response is not a WebSocket upgrade.

    Do NOT add CSP to `/sessions` or `/profiles` machine prefixes.
  </action>
  <verify>pytest backend/tests/test_security_hardening.py -k csp -x</verify>
  <acceptance_criteria>
    - `backend/security_csp.py` exists with functions `viewer_frame_ancestors_header` and `admin_frame_ancestors_none`
    - TestClient GET `/api/status` response headers include `frame-ancestors 'none'`
    - TestClient GET `/sessions` (with valid API key) does NOT include `frame-ancestors 'none'` in response headers
  </acceptance_criteria>
  <done>Admin API responses carry frame-ancestors none; machine routes unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Viewer token auth dependency + machine clipboard GET</name>
  <files>backend/viewer_auth.py, backend/routers/profiles.py, backend/main.py</files>
  <read_first>
    - backend/viewer_tokens.py (from Plan 03-01 — validate API)
    - backend/main.py get_clipboard implementation (reuse read logic)
    - backend/routers/profiles.py
    - .planning/REQUIREMENTS.md SEC-07
  </read_first>
  <action>
    Create `backend/viewer_auth.py`:
    - `async def require_viewer_token(request: Request, profile_id: str) -> dict` — extract token from `X-Viewer-Token` header OR `Authorization: Bearer &lt;token&gt;`; call `viewer_tokens.validate(token, profile_id)`; on failure raise HTTPException(403, detail="Forbidden")

    Add to `profiles_router`:
    - `GET /{profile_id}/clipboard` with `Depends(require_viewer_token)` — reuse clipboard read logic from `main.get_clipboard` (extract shared `_read_profile_clipboard(profile_id)` into `backend/clipboard.py` or importable helper to avoid duplication)

    Ensure route remains behind `require_api_key` on router BUT clipboard handler rejects when only API key present without viewer token — pattern: do NOT list clipboard on router-wide deps only; use `dependencies=[]` on route and check: if `X-Viewer-Token` absent and only API key → 403. **Simpler approach:** clipboard route uses ONLY `Depends(require_viewer_token)` without requiring API key (viewer token is sufficient). SEC-07: API key alone must fail — test with X-API-Key header and no viewer token → 403.

    Leave admin `/api/profiles/{id}/clipboard` unchanged for dashboard.
  </action>
  <verify>pytest backend/tests/test_security_hardening.py -k clipboard -x</verify>
  <acceptance_criteria>
    - `GET /profiles/{pid}/clipboard` with `X-API-Key` only returns HTTP 403
    - Same endpoint with valid `X-Viewer-Token` for that profile returns 200 JSON with `text` key when profile mocked running
    - `require_viewer_token` is defined in `backend/viewer_auth.py`
  </acceptance_criteria>
  <done>Clipboard read scoped to viewer JWT on machine route.</done>
</task>

<task type="auto">
  <name>Task 3: Verify SEC-03 cookie flags (no change if already correct)</name>
  <files>backend/tests/test_security_hardening.py</files>
  <read_first>
    - backend/main.py auth_login set_cookie block (~lines 494-508)
  </read_first>
  <action>
    Add test `test_auth_login_cookie_samesite_strict_httponly`: POST `/api/auth/login` with valid AUTH_TOKEN body; assert `Set-Cookie` contains `HttpOnly` and `SameSite=strict` (case-insensitive). If test fails, add `samesite="strict"` to login/logout cookies — do not change other cookie attributes.

    Document in test docstring that SEC-03 is satisfied by existing implementation when test passes.
  </action>
  <verify>pytest backend/tests/test_security_hardening.py -k cookie -x</verify>
  <acceptance_criteria>
    - Test passes without modifying main.py OR main.py login cookie includes httponly=True and samesite="strict"
  </acceptance_criteria>
  <done>SEC-03 verified by automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main App API key → clipboard | Must not read host clipboard; viewer token required |
| Admin /api → browser | frame-ancestors none prevents clickjacking admin UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Clipboard via API key reads OTP | mitigate | Machine GET /profiles/{id}/clipboard requires viewer JWT; API key only → 403 |
| T-03-06 | Spoofing | Admin UI embedded in malicious iframe | mitigate | CSP frame-ancestors 'none' on /api/* |
| T-03-07 | Spoofing | CSRF on admin cookie | mitigate | SameSite=strict verified on auth_token (SEC-03) |
</threat_model>

<verification>
pytest backend/tests/test_security_hardening.py -x
</verification>

<success_criteria>
- All tests in test_security_hardening.py pass
- SEC-02: /viewer exempt from admin auth by path prefix (document in test or comment — already true for non-/api paths)
</success_criteria>

<output>
After completion, create `.planning/phases/03-signed-viewer-urls-and-security-hardening/03-02-SUMMARY.md`
</output>
