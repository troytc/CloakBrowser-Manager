---
id: 02-06
phase: 02-sessions-warm-pool-and-cdp-lifecycle
plan: 6
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/tests/test_models.py
requirements:
  - SESS-02
  - SESS-13
  - PROF-03
autonomous: true
must_haves:
  truths:
    - "SessionRequest enforces vendor_type and vendor_connection_id are non-empty after whitespace strip; extra='forbid' rejects unknown fields"
    - "SessionResponse contains profile_id, cdp_url, vnc_viewer_url (default empty for Phase 2), state Literal[running, idle, stopped]"
    - "SessionStatusResponse mirrors SessionStatusEnvelope: state Literal, cdp_attach_count int default 0, viewer_attach_count int default 0, idle_expires_at str|None, last_launched_at str|None"
    - "SessionListItem extends SessionStatusResponse with profile_id, vendor_type, vendor_connection_id"
    - "ProfilePatch only allows notes (Phase 2 v1) — clipboard_sync NOT patchable per CLAUDE.md security rule 2; identity keys NOT patchable"
    - "MachineProfileResponse exposes the profile row to /profiles GET endpoints; vendor_type and vendor_connection_id are present and never null"
  artifacts:
    - path: "backend/models.py"
      provides: "SessionRequest, SessionResponse, SessionStatusResponse, SessionListItem, ProfilePatch, MachineProfileResponse"
      contains: "class SessionRequest"
    - path: "backend/tests/test_models.py"
      provides: "Pydantic validation coverage for the 6 new models"
  key_links:
    - from: "backend/models.py::SessionRequest"
      to: "NonEmptyStr alias"
      via: "Annotated[str, StringConstraints(min_length=1, strip_whitespace=True)]"
      pattern: "NonEmptyStr"
    - from: "backend/models.py::ProfilePatch"
      to: "extra=forbid ConfigDict"
      via: "model_config = ConfigDict(extra='forbid')"
      pattern: "extra=.forbid"
---

<objective>
Add the Pydantic v2 model surface that Plans 02-07 (sessions router) and 02-08 (profiles router) wire up. Six new models in `backend/models.py` plus their unit tests. All Phase 2 machine-API request/response shapes are locked here so router work in Wave 3 is mechanical wiring.

Purpose: SESS-02 (response shape), SESS-13 (status envelope shape), PROF-03 (PATCH surface — admin notes only, clipboard_sync excluded per security rule).

Output:
- New models in `backend/models.py`: `NonEmptyStr` type alias, `SessionRequest`, `SessionResponse`, `SessionStatusResponse`, `SessionListItem`, `ProfilePatch`, `MachineProfileResponse`
- Unit tests in `backend/tests/test_models.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-CONTEXT.md
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md
@CLAUDE.md
@backend/models.py

<interfaces>
Existing in backend/models.py (Phase 1):

```
from pydantic import BaseModel, Field, field_validator
from typing import Literal

class ProfileResponse(BaseModel):  # legacy admin shape — stays for Phase 1 routes
    id: str
    name: str
    fingerprint_seed: int
    # ... many fields ...
    status: str = "stopped"
    vnc_ws_port: int | None = None
    cdp_url: str | None = None

class TemplateBlueprint(BaseModel):
    model_config = ConfigDict(extra="forbid")  # exists from Phase 1
    timezone: str | None = None
    # ... blueprint fields ...
    clipboard_sync: bool = False
```

The legacy `ProfileResponse` is consumed by Phase 1 admin routes; Phase 2 keeps it for backward compat and adds NEW machine-API response models alongside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 6 new models + NonEmptyStr alias to backend/models.py</name>
  <files>backend/models.py</files>
  <read_first>
    - backend/models.py entire file (existing imports and the legacy ProfileResponse pattern)
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"Pydantic Model Shapes"
    - CLAUDE.md security rule 2 — clipboard_sync defaults false on every profile, NOT patchable from Main App
  </read_first>
  <behavior>(see Task 2 for the full unit-test set covering these models.)</behavior>
  <action>

Make exactly two edits to backend/models.py.

Edit 1 — Update the imports at the top. Find:

```
from typing import Literal

from pydantic import BaseModel, Field, field_validator
```

Replace with:

```
from typing import Annotated, Literal

from pydantic import BaseModel, ConfigDict, Field, StringConstraints, field_validator
```

If `ConfigDict` is already imported (Phase 1 uses it for `TemplateBlueprint`), check that the line includes all five: `BaseModel`, `ConfigDict`, `Field`, `StringConstraints`, `field_validator`. Add any missing ones. Add `Annotated` to the typing import if not present.

Edit 2 — Append the new models at the END of `backend/models.py` (after `TemplateDeleteBlockedResponse`):

```
# Phase 2 machine-API models (CONTEXT.md D-10 / D-16 / D-18 / D-20)

NonEmptyStr = Annotated[str, StringConstraints(min_length=1, strip_whitespace=True)]


class SessionRequest(BaseModel):
    """POST /sessions request body (SESS-01, SESS-02)."""
    model_config = ConfigDict(extra="forbid")
    vendor_type: NonEmptyStr
    vendor_connection_id: NonEmptyStr


class SessionResponse(BaseModel):
    """POST /sessions response body (SESS-02).

    Phase 2: vnc_viewer_url is the empty string (Phase 3 wires the signed URL).
    """
    model_config = ConfigDict(extra="forbid")
    profile_id: str
    cdp_url: str  # /api/profiles/{id}/cdp (D-13)
    vnc_viewer_url: str = ""  # Phase 3 wires
    state: Literal["running", "idle", "stopped"]


class SessionStatusResponse(BaseModel):
    """GET /sessions/{profile_id} response body (SESS-13, D-18).

    Mirrors SessionStatusEnvelope from session_manager.py.
    """
    model_config = ConfigDict(extra="forbid")
    state: Literal["running", "idle", "stopped"]
    cdp_attach_count: int = 0
    viewer_attach_count: int = 0
    idle_expires_at: str | None = None
    last_launched_at: str | None = None


class SessionListItem(BaseModel):
    """One entry in GET /sessions response (D-14)."""
    model_config = ConfigDict(extra="forbid")
    profile_id: str
    vendor_type: str
    vendor_connection_id: str
    state: Literal["running", "idle", "stopped"]
    cdp_attach_count: int = 0
    viewer_attach_count: int = 0
    idle_expires_at: str | None = None
    last_launched_at: str | None = None


class ProfilePatch(BaseModel):
    """PATCH /profiles/{id} request body (PROF-03).

    Phase 2 v1 surface: notes only. Identity keys (vendor_type,
    vendor_connection_id, template_id) are NOT patchable. clipboard_sync is
    NOT patchable from the Main App per CLAUDE.md security rule 2.
    """
    model_config = ConfigDict(extra="forbid")
    notes: str | None = Field(default=None)


class MachineProfileResponse(BaseModel):
    """GET /profiles, GET /profiles/{id} response body (PROF-01, PROF-02)."""
    model_config = ConfigDict(extra="forbid")
    id: str
    name: str
    vendor_type: str
    vendor_connection_id: str
    template_id: str | None = None
    notes: str | None = None
    user_data_dir: str
    created_at: str
    updated_at: str
    timezone: str | None = None
    locale: str | None = None
    platform: str = "windows"
    screen_width: int = 1920
    screen_height: int = 1080
    clipboard_sync: bool = False

    @field_validator("clipboard_sync", mode="before")
    @classmethod
    def coerce_clipboard_sync(cls, v: object) -> bool:
        return bool(v) if v is not None else False
```

DO NOT modify the legacy `ProfileResponse` — Phase 1 admin routes still consume it; Phase 4 will retire it.
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; python -m pytest backend/tests/test_models.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'class SessionRequest' backend/models.py` matches exactly one line
    - `grep -nE 'class SessionResponse' backend/models.py` matches exactly one line
    - `grep -nE 'class SessionStatusResponse' backend/models.py` matches exactly one line
    - `grep -nE 'class SessionListItem' backend/models.py` matches exactly one line
    - `grep -nE 'class ProfilePatch' backend/models.py` matches exactly one line
    - `grep -nE 'class MachineProfileResponse' backend/models.py` matches exactly one line
    - `grep -nE 'NonEmptyStr = Annotated.str' backend/models.py` matches exactly one line
    - `grep -c 'StringConstraints' backend/models.py` returns at least 1 (in NonEmptyStr) — and import line
    - `grep -c 'class ProfileResponse' backend/models.py` returns 1 (legacy unchanged)
    - The verify command exits 0 (test_models.py from Task 2 passes)
  </acceptance_criteria>
  <done>6 new model classes plus NonEmptyStr alias in backend/models.py; legacy ProfileResponse unchanged; tests in Task 2 pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Pydantic validation tests in backend/tests/test_models.py</name>
  <files>backend/tests/test_models.py</files>
  <read_first>
    - backend/tests/test_models.py current contents (test patterns to follow)
    - backend/models.py post-Task 1 (the 6 new models)
  </read_first>
  <behavior>
    - Test 1 (test_session_request_accepts_valid_input): SessionRequest(vendor_type='acme', vendor_connection_id='user-1') succeeds; both fields equal the input.
    - Test 2 (test_session_request_strips_whitespace): SessionRequest(vendor_type='  acme  ', vendor_connection_id='\tuser-1\n') has stripped values 'acme' and 'user-1' (StringConstraints strip_whitespace=True).
    - Test 3 (test_session_request_rejects_empty_vendor_type): SessionRequest(vendor_type='', vendor_connection_id='user-1') raises ValidationError.
    - Test 4 (test_session_request_rejects_whitespace_only): SessionRequest(vendor_type='   ', vendor_connection_id='user-1') raises ValidationError (after strip → empty → min_length=1 fails).
    - Test 5 (test_session_request_rejects_unknown_field): SessionRequest(vendor_type='acme', vendor_connection_id='u', extra_field='x') raises ValidationError (extra='forbid').
    - Test 6 (test_session_response_default_vnc_viewer_url_is_empty): SessionResponse(profile_id='p1', cdp_url='/api/profiles/p1/cdp', state='running').vnc_viewer_url == ''.
    - Test 7 (test_session_response_rejects_invalid_state): SessionResponse(..., state='unknown') raises ValidationError.
    - Test 8 (test_session_status_response_defaults): SessionStatusResponse(state='stopped') has cdp_attach_count=0, viewer_attach_count=0, idle_expires_at=None, last_launched_at=None.
    - Test 9 (test_profile_patch_accepts_notes_only): ProfilePatch(notes='hello').notes == 'hello'; ProfilePatch().notes is None.
    - Test 10 (test_profile_patch_rejects_clipboard_sync): ProfilePatch(notes='x', clipboard_sync=True) raises ValidationError (extra='forbid').
    - Test 11 (test_profile_patch_rejects_identity_keys): ProfilePatch(notes='x', vendor_type='acme') raises ValidationError; same for vendor_connection_id and template_id.
    - Test 12 (test_machine_profile_response_round_trip): Construct from a dict shaped like database.upsert_profile_by_vendor return; assert all fields populated.
  </behavior>
  <action>

Append the following test block at the end of `backend/tests/test_models.py`. If the file is empty or doesn't exist, create it with the imports.

```
"""Phase 2 machine-API model validation tests (SESS-02, SESS-13, PROF-03)."""

import pytest
from pydantic import ValidationError

from backend.models import (
    MachineProfileResponse,
    ProfilePatch,
    SessionListItem,
    SessionRequest,
    SessionResponse,
    SessionStatusResponse,
)


def test_session_request_accepts_valid_input():
    sr = SessionRequest(vendor_type="acme", vendor_connection_id="user-1")
    assert sr.vendor_type == "acme"
    assert sr.vendor_connection_id == "user-1"


def test_session_request_strips_whitespace():
    sr = SessionRequest(vendor_type="  acme  ", vendor_connection_id="\tuser-1\n")
    assert sr.vendor_type == "acme"
    assert sr.vendor_connection_id == "user-1"


def test_session_request_rejects_empty_vendor_type():
    with pytest.raises(ValidationError):
        SessionRequest(vendor_type="", vendor_connection_id="user-1")


def test_session_request_rejects_whitespace_only():
    with pytest.raises(ValidationError):
        SessionRequest(vendor_type="   ", vendor_connection_id="user-1")


def test_session_request_rejects_unknown_field():
    with pytest.raises(ValidationError):
        SessionRequest(
            vendor_type="acme",
            vendor_connection_id="user-1",
            extra_field="x",
        )


def test_session_response_default_vnc_viewer_url_is_empty():
    resp = SessionResponse(
        profile_id="p1",
        cdp_url="/api/profiles/p1/cdp",
        state="running",
    )
    assert resp.vnc_viewer_url == ""


def test_session_response_rejects_invalid_state():
    with pytest.raises(ValidationError):
        SessionResponse(
            profile_id="p1",
            cdp_url="/api/profiles/p1/cdp",
            state="unknown",
        )


def test_session_status_response_defaults():
    s = SessionStatusResponse(state="stopped")
    assert s.cdp_attach_count == 0
    assert s.viewer_attach_count == 0
    assert s.idle_expires_at is None
    assert s.last_launched_at is None


def test_session_list_item_round_trip():
    item = SessionListItem(
        profile_id="p1",
        vendor_type="acme",
        vendor_connection_id="u1",
        state="running",
        cdp_attach_count=2,
        viewer_attach_count=0,
    )
    assert item.cdp_attach_count == 2
    assert item.state == "running"


def test_profile_patch_accepts_notes_only():
    p = ProfilePatch(notes="hello")
    assert p.notes == "hello"
    p_empty = ProfilePatch()
    assert p_empty.notes is None


def test_profile_patch_rejects_clipboard_sync():
    with pytest.raises(ValidationError):
        ProfilePatch(notes="x", clipboard_sync=True)


def test_profile_patch_rejects_identity_keys():
    with pytest.raises(ValidationError):
        ProfilePatch(notes="x", vendor_type="acme")
    with pytest.raises(ValidationError):
        ProfilePatch(notes="x", vendor_connection_id="u")
    with pytest.raises(ValidationError):
        ProfilePatch(notes="x", template_id="t")


def test_machine_profile_response_round_trip():
    payload = {
        "id": "p1",
        "name": "acme/u1",
        "vendor_type": "acme",
        "vendor_connection_id": "u1",
        "template_id": "t1",
        "notes": None,
        "user_data_dir": "/data/profiles/p1",
        "created_at": "2026-05-09T00:00:00+00:00",
        "updated_at": "2026-05-09T00:00:00+00:00",
        "timezone": "UTC",
        "locale": "en-US",
        "platform": "windows",
        "screen_width": 1920,
        "screen_height": 1080,
        "clipboard_sync": 0,
    }
    m = MachineProfileResponse(**payload)
    assert m.vendor_type == "acme"
    assert m.vendor_connection_id == "u1"
    assert m.clipboard_sync is False
```
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; pytest backend/tests/test_models.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_models.py exists
    - `grep -c '^def test_session_request_\|^def test_session_response_\|^def test_session_status_response_\|^def test_session_list_item_\|^def test_profile_patch_\|^def test_machine_profile_response_' backend/tests/test_models.py` returns at least 12
    - `pytest backend/tests/test_models.py -x` exits 0
    - `pytest backend/tests/test_models.py -k "patch_rejects" -v` shows 3 tests passing (clipboard_sync, identity-keys variants)
  </acceptance_criteria>
  <done>12 model validation tests pass; clipboard_sync rejection on ProfilePatch is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main App POST body -> SessionRequest validation | extra='forbid' rejects unknown fields; whitespace-stripped non-empty vendor identifiers prevent bypass |
| Main App PATCH body -> ProfilePatch validation | clipboard_sync and identity keys explicitly excluded; extra='forbid' enforces — Pydantic is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering / Information Disclosure | ProfilePatch accepting clipboard_sync flip | mitigate | ProfilePatch model has only `notes` field; ConfigDict(extra='forbid') rejects ANY other field including clipboard_sync — verified by test_profile_patch_rejects_clipboard_sync |
| T-02-16 | Tampering | ProfilePatch accepting identity-key change (vendor_type, vendor_connection_id, template_id) | mitigate | Same ConfigDict(extra='forbid'); identity keys also excluded explicitly — verified by test_profile_patch_rejects_identity_keys |
| T-02-17 (input validation) | Tampering | SessionRequest accepting whitespace-only vendor identifiers | mitigate | NonEmptyStr = Annotated[str, StringConstraints(min_length=1, strip_whitespace=True)] — strip happens BEFORE the min_length check |
</threat_model>

<verification>
- All 12 unit tests in test_models.py pass
- `python -c "from backend.models import SessionRequest, SessionResponse, SessionStatusResponse, SessionListItem, ProfilePatch, MachineProfileResponse, NonEmptyStr"` exits 0
- Legacy ProfileResponse is unchanged (Phase 1 admin route compatibility)
- ProfilePatch rejects clipboard_sync (security rule 2 invariant)
</verification>

<success_criteria>
- `pytest backend/tests/test_models.py -x` exits 0
- All 6 new model classes exist and are importable
- ProfilePatch rejects unknown fields including clipboard_sync, vendor_type, vendor_connection_id, template_id
</success_criteria>

<output>
After completion, create `.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-06-SUMMARY.md` describing:
- The 6 new model class names + NonEmptyStr alias
- Confirmation that legacy ProfileResponse is unchanged
- Confirmation that ProfilePatch.notes is the only writable Phase 2 field
- The 12 test names that pass
</output>
