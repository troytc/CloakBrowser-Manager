---
id: 02-02
phase: 02-sessions-warm-pool-and-cdp-lifecycle
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/auth_api_key.py
  - backend/main.py
  - backend/tests/test_auth_api_key.py
requirements:
  - SEC-01
autonomous: true
must_haves:
  truths:
    - "Machine routes (/sessions/*, /profiles/*) are gated by APIKeyHeader-based Depends(require_api_key) — no admin cookie path"
    - "require_api_key reads MAIN_APP_API_KEY at REQUEST TIME via os.environ (not at import) so monkeypatch.setenv in tests works"
    - "Missing or wrong X-API-Key returns HTTPException(401, detail='Invalid or missing API key', headers={'WWW-Authenticate': 'ApiKey'})"
    - "Comparison uses hmac.compare_digest, never == (timing-attack resistant; matches existing main.py:103,114 pattern)"
    - "DEV_MODE=1 with MAIN_APP_API_KEY unset returns 'dev-mode' string (bypass) — production with key unset still 401s"
    - "AuthMiddleware in main.py adds /sessions and /profiles prefix exemption + CDP WS bypass; admin cookie path untouched for everything else"
    - "CDP WS handlers /api/profiles/{id}/cdp and /api/profiles/{id}/cdp/devtools/{path} validate X-API-Key BEFORE websocket.accept() and close(4401) on miss"
  artifacts:
    - path: "backend/auth_api_key.py"
      provides: "require_api_key dependency, APIKeyHeader instance, _expected_key/_dev_mode helpers"
      exports: ["require_api_key"]
    - path: "backend/main.py"
      provides: "_AUTH_EXEMPT_PREFIXES tuple; CDP WS bypass logic in AuthMiddleware; _ws_api_key_valid helper; X-API-Key check at top of cdp_proxy and cdp_page_proxy"
      contains: "_ws_api_key_valid"
    - path: "backend/tests/test_auth_api_key.py"
      provides: "Unit coverage for missing/empty/correct/wrong key, DEV_MODE bypass, env-at-request-time"
  key_links:
    - from: "backend/auth_api_key.py::require_api_key"
      to: "backend/auth_api_key.py::_expected_key"
      via: "os.environ.get('MAIN_APP_API_KEY') read at request time"
      pattern: "os.environ.get.*MAIN_APP_API_KEY"
    - from: "backend/main.py::AuthMiddleware.__call__"
      to: "_AUTH_EXEMPT_PREFIXES"
      via: "any(path.startswith(p) for p in _AUTH_EXEMPT_PREFIXES) check before _check_auth"
      pattern: "_AUTH_EXEMPT_PREFIXES"
    - from: "backend/main.py::cdp_proxy"
      to: "_ws_api_key_valid"
      via: "called BEFORE await websocket.accept()"
      pattern: "_ws_api_key_valid"
---

<objective>
Stand up the machine-API authentication surface (SEC-01). Create `backend/auth_api_key.py` with the `require_api_key` Depends-compatible coroutine that validates `X-API-Key` against `MAIN_APP_API_KEY` via `hmac.compare_digest`, with DEV_MODE bypass. Update `backend/main.py`'s `AuthMiddleware` to exempt `/sessions` and `/profiles` path prefixes (D-12). Add an `X-API-Key` validation helper for the existing CDP WS handlers (`/api/profiles/{id}/cdp` and `/api/profiles/{id}/cdp/devtools/{path}`) so the Main App (no admin cookie) can connect — resolving Pre-Phase Lookup L-03.

Purpose: SEC-01 is the gatekeeper for every Phase 2 machine route. Without this plan, the new sessions and profiles routers (Plans 02-07, 02-08) cannot enforce auth, and the CDP WS proxy stays admin-only.

Output:
- New module `backend/auth_api_key.py`
- `_AUTH_EXEMPT_PREFIXES` tuple in `main.py` and updated `AuthMiddleware.__call__`
- `_ws_api_key_valid(websocket)` helper in `main.py`; called at the top of `cdp_proxy` and `cdp_page_proxy` BEFORE `websocket.accept()`
- Unit tests in `backend/tests/test_auth_api_key.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-CONTEXT.md
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md
@CLAUDE.md
@backend/main.py
@backend/tests/conftest.py

<interfaces>
Existing in backend/main.py — DO NOT change these contracts:

```
# Module-level config (lines 55-89, after Phase 1):
VIEWER_SECRET: str | None = os.environ.get("VIEWER_SECRET")
MAIN_APP_API_KEY: str | None = os.environ.get("MAIN_APP_API_KEY")
DEV_MODE: bool = os.environ.get("DEV_MODE", "").strip().lower() in ("1", "true", "yes")

def _check_required_env() -> None: ...  # raises in production if vars missing

# Existing exemptions (line 92):
_AUTH_EXEMPT = frozenset({"/api/auth/status", "/api/auth/login", "/api/status"})

# Existing AuthMiddleware (line 177-211):
class AuthMiddleware:
    async def __call__(self, scope, receive, send):
        if not AUTH_TOKEN or scope["type"] not in ("http", "websocket"):
            await self.app(scope, receive, send); return
        path = scope["path"]
        if path in _AUTH_EXEMPT or not path.startswith("/api/"):
            await self.app(scope, receive, send); return
        if _check_auth(scope):
            await self.app(scope, receive, send); return
        # 401 reject

# Existing CDP WS handlers (lines 1007-1049):
@app.websocket("/api/profiles/{profile_id}/cdp")
async def cdp_proxy(websocket: WebSocket, profile_id: str):
    if not await _check_websocket_origin(websocket): return
    running = browser_mgr.running.get(profile_id)
    if not running:
        await websocket.close(code=4004, reason="Profile not running"); return
    await websocket.accept()
    # ... existing CDP proxy logic ...
```

The existing `_check_auth` (line 95) reads `Authorization: Bearer` and `auth_token` cookie — leave it untouched. We're adding a SEPARATE auth surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create backend/auth_api_key.py module + unit tests</name>
  <files>backend/auth_api_key.py, backend/tests/test_auth_api_key.py</files>
  <read_first>
    - backend/main.py lines 92-118 (existing _check_auth function — pattern reference for hmac.compare_digest usage)
    - backend/tests/conftest.py (DEV_MODE setup at line 21; mock cloakbrowser pattern at lines 30-37)
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"9. APIKeyHeader Implementation"
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"Pitfall E: Reading MAIN_APP_API_KEY at module import" — DO NOT capture env var at import time
  </read_first>
  <behavior>
    - Test 1 (test_require_api_key_accepts_correct_key): With MAIN_APP_API_KEY=secret123 and DEV_MODE unset, await require_api_key(api_key="secret123") returns "secret123" with no exception.
    - Test 2 (test_require_api_key_rejects_wrong_key): With MAIN_APP_API_KEY=secret123, await require_api_key(api_key="wrong") raises HTTPException with status_code=401, detail="Invalid or missing API key", and headers["WWW-Authenticate"] == "ApiKey".
    - Test 3 (test_require_api_key_rejects_missing_key): With MAIN_APP_API_KEY=secret123, await require_api_key(api_key=None) raises 401.
    - Test 4 (test_require_api_key_rejects_empty_key): With MAIN_APP_API_KEY=secret123, await require_api_key(api_key="") raises 401.
    - Test 5 (test_require_api_key_dev_mode_bypass): With MAIN_APP_API_KEY UNSET and DEV_MODE=1, await require_api_key(api_key=None) returns "dev-mode".
    - Test 6 (test_require_api_key_production_unset_still_401s): With MAIN_APP_API_KEY UNSET and DEV_MODE not set, raises 401 (defense in depth).
    - Test 7 (test_require_api_key_reads_env_at_request_time): Set MAIN_APP_API_KEY="A". Import the module. Reset MAIN_APP_API_KEY="B" via monkeypatch. Call require_api_key("B") and assert it succeeds.
  </behavior>
  <action>

Part A — Create `backend/auth_api_key.py` with this exact content:

```
"""Machine-API authentication via X-API-Key header (SEC-01, CONTEXT.md D-11).

Mounted on routers/sessions.py and routers/profiles.py:

    from .auth_api_key import require_api_key
    router = APIRouter(prefix="/sessions", dependencies=[Depends(require_api_key)])

DO NOT use this on /admin/* — that's the existing AuthMiddleware surface
(CLAUDE.md security rule 3: two auth surfaces, strictly segregated).

The MAIN_APP_API_KEY value is read at REQUEST time, not at module import,
so test fixtures that monkeypatch.setenv work correctly (RESEARCH.md
Pitfall E).
"""

from __future__ import annotations

import hmac
import logging
import os

from fastapi import HTTPException, Security
from fastapi.security import APIKeyHeader

logger = logging.getLogger("vendorbrowser.auth_api_key")

# auto_error=False so we raise our own 401 with sanitized body and the
# WWW-Authenticate header (RFC 7235).
_api_key_header = APIKeyHeader(name="X-API-Key", auto_error=False)


def _expected_key() -> str | None:
    """Read MAIN_APP_API_KEY at request time (NOT module import).
    Strips whitespace; treats empty string as None."""
    raw = os.environ.get("MAIN_APP_API_KEY") or ""
    stripped = raw.strip()
    return stripped or None


def _dev_mode() -> bool:
    """Match main.py:59 detection so the two stay in sync."""
    return os.environ.get("DEV_MODE", "").strip().lower() in ("1", "true", "yes")


async def require_api_key(api_key: str | None = Security(_api_key_header)) -> str:
    """Validate X-API-Key against MAIN_APP_API_KEY (constant-time compare)."""
    expected = _expected_key()

    if expected is None:
        if _dev_mode():
            logger.debug("DEV_MODE bypass for X-API-Key check")
            return "dev-mode"
        raise HTTPException(
            status_code=401,
            detail="Invalid or missing API key",
            headers={"WWW-Authenticate": "ApiKey"},
        )

    if not api_key:
        raise HTTPException(
            status_code=401,
            detail="Invalid or missing API key",
            headers={"WWW-Authenticate": "ApiKey"},
        )

    if not hmac.compare_digest(api_key.encode("utf-8"), expected.encode("utf-8")):
        raise HTTPException(
            status_code=401,
            detail="Invalid or missing API key",
            headers={"WWW-Authenticate": "ApiKey"},
        )
    return api_key
```

Part B — Create `backend/tests/test_auth_api_key.py` with this exact content:

```
"""Unit tests for backend/auth_api_key.py (SEC-01)."""

from __future__ import annotations

import pytest
from fastapi import HTTPException

from backend.auth_api_key import require_api_key


@pytest.mark.asyncio
async def test_require_api_key_accepts_correct_key(monkeypatch):
    monkeypatch.setenv("MAIN_APP_API_KEY", "secret123")
    monkeypatch.delenv("DEV_MODE", raising=False)
    result = await require_api_key(api_key="secret123")
    assert result == "secret123"


@pytest.mark.asyncio
async def test_require_api_key_rejects_wrong_key(monkeypatch):
    monkeypatch.setenv("MAIN_APP_API_KEY", "secret123")
    monkeypatch.delenv("DEV_MODE", raising=False)
    with pytest.raises(HTTPException) as exc_info:
        await require_api_key(api_key="wrong")
    assert exc_info.value.status_code == 401
    assert exc_info.value.detail == "Invalid or missing API key"
    assert exc_info.value.headers == {"WWW-Authenticate": "ApiKey"}


@pytest.mark.asyncio
async def test_require_api_key_rejects_missing_key(monkeypatch):
    monkeypatch.setenv("MAIN_APP_API_KEY", "secret123")
    monkeypatch.delenv("DEV_MODE", raising=False)
    with pytest.raises(HTTPException) as exc_info:
        await require_api_key(api_key=None)
    assert exc_info.value.status_code == 401


@pytest.mark.asyncio
async def test_require_api_key_rejects_empty_key(monkeypatch):
    monkeypatch.setenv("MAIN_APP_API_KEY", "secret123")
    monkeypatch.delenv("DEV_MODE", raising=False)
    with pytest.raises(HTTPException) as exc_info:
        await require_api_key(api_key="")
    assert exc_info.value.status_code == 401


@pytest.mark.asyncio
async def test_require_api_key_dev_mode_bypass(monkeypatch):
    monkeypatch.delenv("MAIN_APP_API_KEY", raising=False)
    monkeypatch.setenv("DEV_MODE", "1")
    result = await require_api_key(api_key=None)
    assert result == "dev-mode"


@pytest.mark.asyncio
async def test_require_api_key_production_unset_still_401s(monkeypatch):
    monkeypatch.delenv("MAIN_APP_API_KEY", raising=False)
    monkeypatch.delenv("DEV_MODE", raising=False)
    with pytest.raises(HTTPException) as exc_info:
        await require_api_key(api_key="anything")
    assert exc_info.value.status_code == 401


@pytest.mark.asyncio
async def test_require_api_key_reads_env_at_request_time(monkeypatch):
    monkeypatch.setenv("MAIN_APP_API_KEY", "A")
    monkeypatch.delenv("DEV_MODE", raising=False)
    assert await require_api_key(api_key="A") == "A"
    monkeypatch.setenv("MAIN_APP_API_KEY", "B")
    assert await require_api_key(api_key="B") == "B"
    with pytest.raises(HTTPException):
        await require_api_key(api_key="A")
```

Note on conftest.py: The existing conftest.py sets DEV_MODE=1 via os.environ.setdefault (line 21). Use monkeypatch.delenv("DEV_MODE", raising=False) in tests that need DEV_MODE off — monkeypatch reverts at teardown so the conftest default returns. Do NOT modify conftest.
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; pytest backend/tests/test_auth_api_key.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File backend/auth_api_key.py exists
    - `grep -nE 'async def require_api_key' backend/auth_api_key.py` matches exactly one line
    - `grep -nE 'APIKeyHeader.name="X-API-Key"' backend/auth_api_key.py` matches exactly one line
    - `grep -nE 'hmac.compare_digest' backend/auth_api_key.py` matches exactly one line
    - `grep -nE 'def _expected_key' backend/auth_api_key.py` matches exactly one line
    - `grep -c 'os.environ.get' backend/auth_api_key.py` returns at least 2
    - `grep -c 'WWW-Authenticate' backend/auth_api_key.py` returns at least 3
    - File backend/tests/test_auth_api_key.py exists
    - `pytest backend/tests/test_auth_api_key.py -x` exits 0
    - `pytest backend/tests/test_auth_api_key.py -x -v` reports 7 tests passed
  </acceptance_criteria>
  <done>auth_api_key.py module exists with require_api_key dependency; 7 unit tests pass; env var read at request time confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update AuthMiddleware exemptions and add CDP WS X-API-Key validation in main.py</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py lines 92-118 (existing _AUTH_EXEMPT and _check_auth)
    - backend/main.py lines 177-211 (AuthMiddleware.__call__ — pattern to extend)
    - backend/main.py lines 127-174 (existing _check_websocket_origin — header iteration pattern to copy for _ws_api_key_valid)
    - backend/main.py lines 1007-1049 (cdp_proxy and cdp_page_proxy — both need X-API-Key validation BEFORE accept())
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"L-03 Decision" + §"1. CDP WS Auth Seam"
  </read_first>
  <behavior>
    - Test 1 (manually verifiable, full coverage in Plan 02-07/02-09): A POST to /sessions with no Authorization header bypasses AuthMiddleware (because /sessions is in _AUTH_EXEMPT_PREFIXES); the router-level Depends(require_api_key) then enforces auth.
    - Test 2 (admin cookie still works for /admin/*): No regression — existing /api/auth/login, /api/profiles (legacy), /api/templates paths still flow through AuthMiddleware unchanged.
    - Test 3 (_ws_api_key_valid reads x-api-key from websocket.scope.headers and compares with hmac.compare_digest): symbol-existence check covered by acceptance criteria; functional WS test deferred to Plan 02-09.
  </behavior>
  <action>

Make exactly these edits to backend/main.py — do NOT touch any other code.

Edit 1 — After the existing _AUTH_EXEMPT line (line 92), add the prefix list and CDP-WS bypass marker.

Find:
```
_AUTH_EXEMPT = frozenset({"/api/auth/status", "/api/auth/login", "/api/status"})
```

Add directly below:
```
# Phase 2 (CONTEXT.md D-12, SEC-01): machine API path prefixes
# /sessions and /profiles are gated by Depends(require_api_key) at the router
# level (not by AuthMiddleware), so they bypass the admin-cookie check here.
# CLAUDE.md security rule 3: two auth surfaces, strictly segregated by prefix.
_AUTH_EXEMPT_PREFIXES: tuple[str, ...] = ("/sessions", "/profiles")

# Phase 2 (RESEARCH.md L-03): CDP WS bypass for AuthMiddleware
# /api/profiles/{id}/cdp and /api/profiles/{id}/cdp/devtools/{path} need to be
# reachable by the Main App (which has X-API-Key, not the admin cookie).
# AuthMiddleware lets them through; the handlers themselves call
# _ws_api_key_valid() before websocket.accept().
_CDP_WS_PATH_INFIX: str = "/cdp"
_CDP_WS_PATH_PREFIX: str = "/api/profiles/"
```

Edit 2 — Update AuthMiddleware.__call__ (line 187 onward).

Find this block inside AuthMiddleware.__call__:
```
        if path in _AUTH_EXEMPT or not path.startswith("/api/"):
            await self.app(scope, receive, send)
            return
```

Replace with:
```
        # Phase 2 (D-12): exempt machine-API prefixes (router-level auth gates)
        if any(path.startswith(p) for p in _AUTH_EXEMPT_PREFIXES):
            await self.app(scope, receive, send)
            return

        # Phase 2 (L-03): exempt CDP WS upgrade paths so the Main App can
        # connect via X-API-Key (handler validates inside, before accept()).
        if (
            scope["type"] == "websocket"
            and path.startswith(_CDP_WS_PATH_PREFIX)
            and _CDP_WS_PATH_INFIX in path
        ):
            await self.app(scope, receive, send)
            return

        if path in _AUTH_EXEMPT or not path.startswith("/api/"):
            await self.app(scope, receive, send)
            return
```

Edit 3 — Add _ws_api_key_valid helper. Place directly after the existing _check_websocket_origin function (around line 174):

```
def _ws_api_key_valid(websocket: WebSocket) -> bool:
    """Validate X-API-Key on WS upgrade scope BEFORE await websocket.accept().

    Mirrors backend.auth_api_key.require_api_key for the WebSocket case
    (browser WebSocket APIs cannot send custom headers, but server-side
    Playwright clients CAN — the Main App is server-side; RESEARCH.md A4).

    Returns True on valid X-API-Key (or DEV_MODE bypass with no key set);
    False otherwise. Caller is responsible for closing the WS with 4401.
    """
    expected_raw = os.environ.get("MAIN_APP_API_KEY") or ""
    expected = expected_raw.strip()
    dev_mode = os.environ.get("DEV_MODE", "").strip().lower() in ("1", "true", "yes")

    if not expected:
        return dev_mode

    for key, val in websocket.scope.get("headers", []):
        if key == b"x-api-key":
            try:
                provided = val.decode("latin-1").strip()
            except UnicodeDecodeError:
                return False
            return hmac.compare_digest(
                provided.encode("utf-8"),
                expected.encode("utf-8"),
            )
    return False
```

Edit 4 — Add X-API-Key check at the top of cdp_proxy (line 1008) BEFORE await websocket.accept(). Insert the check between _check_websocket_origin and the running lookup:

```
@app.websocket("/api/profiles/{profile_id}/cdp")
async def cdp_proxy(websocket: WebSocket, profile_id: str):
    """Proxy WebSocket frames between external tools and Chrome's CDP."""
    if not await _check_websocket_origin(websocket):
        return

    # Phase 2 (SEC-01, L-03): validate X-API-Key BEFORE accept().
    # Server-side Playwright clients send extra_headers; browsers cannot.
    if not _ws_api_key_valid(websocket):
        await websocket.close(code=4401, reason="Invalid or missing API key")
        return

    running = browser_mgr.running.get(profile_id)
    # ... rest unchanged ...
```

Edit 5 — Same X-API-Key check at the top of cdp_page_proxy (line 1036) BEFORE await websocket.accept(). The same insertion: after _check_websocket_origin returns true, insert the _ws_api_key_valid block. Identical to Edit 4 mechanics.

DO NOT touch the /api/profiles/{id}/vnc admin VNC handler (line 710) — that is admin-only, gated by AuthMiddleware. It must NOT pick up the CDP WS bypass.
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; python -c "import sys; [sys.modules.pop(m) for m in list(sys.modules) if m.startswith('backend.')]; from backend import main; assert hasattr(main, '_AUTH_EXEMPT_PREFIXES'); assert main._AUTH_EXEMPT_PREFIXES == ('/sessions', '/profiles'); assert hasattr(main, '_ws_api_key_valid'); assert hasattr(main, '_CDP_WS_PATH_PREFIX'); assert main._CDP_WS_PATH_PREFIX == '/api/profiles/'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '_AUTH_EXEMPT_PREFIXES.*tuple' backend/main.py` matches exactly one line
    - `grep -nE '_AUTH_EXEMPT_PREFIXES.*=.*."/sessions",.*"/profiles"' backend/main.py` matches exactly one line
    - `grep -nE 'def _ws_api_key_valid' backend/main.py` matches exactly one line
    - `grep -c 'any\(path.startswith\(p\) for p in _AUTH_EXEMPT_PREFIXES\)' backend/main.py` returns at least 1
    - `grep -c '_ws_api_key_valid(websocket)' backend/main.py` returns at least 2 (called in cdp_proxy AND cdp_page_proxy)
    - `grep -c 'code=4401, reason="Invalid or missing API key"' backend/main.py` returns at least 2
    - `python -c "import sys; [sys.modules.pop(m) for m in list(sys.modules) if m.startswith('backend.')]; from backend import main; assert hasattr(main, '_AUTH_EXEMPT_PREFIXES')"` exits 0
    - `pytest backend/tests/test_api.py backend/tests/test_auth.py -x` exits 0 (no regressions to existing admin/auth tests)
  </acceptance_criteria>
  <done>AuthMiddleware exempts /sessions and /profiles prefixes plus CDP WS paths; _ws_api_key_valid helper exists and is called BEFORE accept() in both CDP WS handlers; existing admin auth tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main App (untrusted client) -> machine API (/sessions/*, /profiles/*) | Crosses here via X-API-Key header; constant-time compare prevents timing attacks |
| Main App -> CDP WebSocket /api/profiles/{id}/cdp | WS upgrade scope carries X-API-Key; validated before accept() so unauth attempts never reach proxy logic |
| Admin browser (cookie auth) -> /admin/* and /api/templates | Continues through AuthMiddleware unchanged; strictly segregated path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | Machine routes (/sessions, /profiles) accepting unauthenticated requests | mitigate | Router-level dependencies=[Depends(require_api_key)] in Plans 02-07/02-08; AuthMiddleware exempts /sessions and /profiles prefixes so router-level Depends is the sole gate (no double-auth confusion); 401 with WWW-Authenticate: ApiKey on miss |
| T-02-07 | Elevation of Privilege | CDP WS reachable via admin cookie bypass for Main App | mitigate | _ws_api_key_valid called BEFORE websocket.accept() in both cdp_proxy and cdp_page_proxy; close(4401) on miss; AuthMiddleware bypass for CDP WS paths is intentional so Main App can connect |
| T-02-08 (defense in depth) | Information Disclosure | Timing attack on hmac comparison reveals key bytes | mitigate | hmac.compare_digest used in both require_api_key and _ws_api_key_valid; matches existing pattern at main.py:103,114 |
| T-02-09 (defense in depth) | Information Disclosure | Identical 401 detail string on every failure mode prevents leaking which check failed | mitigate | All 401s in require_api_key emit detail="Invalid or missing API key" with headers={"WWW-Authenticate": "ApiKey"} — no per-failure-mode discrimination |
</threat_model>

<verification>
- `pytest backend/tests/test_auth_api_key.py -x` passes (7 new tests)
- `pytest backend/tests/test_auth.py backend/tests/test_api.py -x` still passes (no regression to admin auth)
- `_AUTH_EXEMPT` (the original frozenset) still contains exactly the original 3 paths — no entries added there; the new exemptions live in `_AUTH_EXEMPT_PREFIXES`
- `_check_auth` (existing function for admin cookie/Bearer) is byte-for-byte unchanged
- `/api/profiles/{id}/vnc` (admin VNC handler at line 710) is NOT modified — still gated by AuthMiddleware admin auth
</verification>

<success_criteria>
- All 7 unit tests in test_auth_api_key.py pass
- `python -c "from backend.auth_api_key import require_api_key; print('importable')"` exits 0
- `python -c "import sys; [sys.modules.pop(m) for m in list(sys.modules) if m.startswith('backend.')]; from backend import main; assert main._AUTH_EXEMPT_PREFIXES == ('/sessions', '/profiles')"` exits 0
- `grep -c '_ws_api_key_valid(websocket)' backend/main.py` returns 2 (one call in each CDP WS handler)
- Existing admin auth tests (`test_auth.py`) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-02-SUMMARY.md` describing:
- Final shape of `backend/auth_api_key.py` (line count, public exports)
- Exact lines in `main.py` where `_AUTH_EXEMPT_PREFIXES`, `_ws_api_key_valid`, and the AuthMiddleware update landed
- Confirmation that existing admin auth (`_check_auth`) is unchanged
- Confirmation that admin VNC handler (`/api/profiles/{id}/vnc`) is unchanged
</output>
