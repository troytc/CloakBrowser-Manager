---
id: 02-03
phase: 02-sessions-warm-pool-and-cdp-lifecycle
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database.py
  - backend/tests/test_database.py
requirements:
  - SESS-01
  - SESS-08
  - PROF-02
autonomous: true
must_haves:
  truths:
    - "upsert_profile_by_vendor(vendor_type, vendor_connection_id) is SELECT then create_profile_from_template (which INSERT OR ABORT) then re-SELECT inside a single transaction sequence"
    - "Existing row for the (vendor_type, vendor_connection_id) pair is returned without re-snapshotting (template is never re-read at wake)"
    - "Missing template raises NoTemplateError(vendor_type) — bubbles up to /sessions router as 404"
    - "sqlite3.IntegrityError from concurrent INSERT (cross-process race) is swallowed and a re-SELECT returns the winner's row"
    - "list_profiles_filtered(vendor_type=None, vendor_connection_id=None) returns [] (not 404) when no match — matches D-20"
    - "Existing UNIQUE(vendor_type, vendor_connection_id) index from Phase 1 is the cross-process safety net (verified by tests)"
  artifacts:
    - path: "backend/database.py"
      provides: "NoTemplateError exception, upsert_profile_by_vendor() function, list_profiles_filtered() helper"
      contains: "def upsert_profile_by_vendor"
    - path: "backend/tests/test_database.py"
      provides: "Unit coverage for upsert happy path / existing row / missing template / IntegrityError swallow / list_profiles_filtered all combinations"
  key_links:
    - from: "backend/database.py::upsert_profile_by_vendor"
      to: "backend/database.py::create_profile_from_template"
      via: "direct call when no existing row found"
      pattern: "create_profile_from_template"
    - from: "backend/database.py::upsert_profile_by_vendor"
      to: "sqlite3.IntegrityError"
      via: "try/except around create_profile_from_template, then re-SELECT"
      pattern: "except sqlite3.IntegrityError"
---

<objective>
Add the data-layer upsert primitive that the SessionManager (Plan 02-04) and POST /sessions handler (Plan 02-07) call into. Implements the SELECT then INSERT OR ABORT then re-SELECT pattern from D-15 and the cross-process IntegrityError safety net. Also adds `list_profiles_filtered()` for PROF-01/PROF-02 (the profiles router in Plan 02-08 wraps it).

Purpose: SESS-01 (idempotent upsert) lives in two layers — `asyncio.Lock` per key serializes within process (Plan 02-04), `UNIQUE` constraint + INSERT OR ABORT catches the cross-process case here. SESS-08 (UNIQUE at DB layer) is already in place from Phase 1; this plan verifies it via tests and uses it.

Output:
- `NoTemplateError` exception class in `backend/database.py`
- `upsert_profile_by_vendor(vendor_type, vendor_connection_id) -> dict` function
- `list_profiles_filtered(vendor_type=None, vendor_connection_id=None) -> list[dict]` helper
- Unit tests in `backend/tests/test_database.py` covering all paths
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-CONTEXT.md
@.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md
@CLAUDE.md
@backend/database.py

<interfaces>
Existing in backend/database.py — DO NOT change these contracts:

```
@contextmanager
def get_db(): ...  # yields sqlite3.Connection with row_factory=sqlite3.Row, WAL mode

def init_db() -> None: ...  # creates tables, runs migrations, creates UNIQUE INDEX idx_profiles_vendor_pair

def get_profile(profile_id: str) -> dict | None: ...
def list_profiles() -> list[dict]: ...  # all profiles, no filter — Phase 1 helper
def update_profile(profile_id: str, **fields) -> dict | None: ...
def delete_profile(profile_id: str) -> bool: ...

def get_template_by_vendor_type(vendor_type: str) -> dict | None: ...
def create_profile_from_template(
    template: dict,
    vendor_connection_id: str,
    name: str | None = None,
) -> dict: ...  # snapshots blueprint into new profile row; raises sqlite3.IntegrityError on UNIQUE violation
```

Key facts about `create_profile_from_template`:
- Reads `template["blueprint"]` (JSON string in DB → loads into dict)
- INSERTs profiles row with vendor_type, vendor_connection_id, template_id columns
- Returns `get_profile(profile_id)` on success
- Default `INSERT OR ABORT` semantics — raises `sqlite3.IntegrityError` on UNIQUE constraint violation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NoTemplateError + upsert_profile_by_vendor() + list_profiles_filtered() to backend/database.py</name>
  <files>backend/database.py</files>
  <read_first>
    - backend/database.py entire file (note the existing helpers: get_profile at line 207, list_profiles at line 222, get_template_by_vendor_type at line 316, create_profile_from_template at line 392)
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"4. upsert_profile_by_vendor — Concrete SQL + Python" — concrete sketch
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"14. GET /profiles, PATCH /profiles, DELETE /profiles" — list_profiles_filtered helper sketch
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"Pitfall F: INSERT OR IGNORE swallowing the conflict silently" — DO NOT use OR IGNORE
  </read_first>
  <behavior>
    - Test 1 (test_upsert_creates_new_profile_when_template_exists): Seed a vendor_template via create_template. Call upsert_profile_by_vendor("acme", "user-1"). Assert a profile is returned with vendor_type="acme", vendor_connection_id="user-1", template_id == seeded template id, and snapshot fields from the blueprint (e.g., timezone, locale).
    - Test 2 (test_upsert_returns_existing_profile_unchanged): After Test 1's create, call upsert_profile_by_vendor("acme", "user-1") AGAIN. Assert: same profile_id, no new row in profiles table (count is 1), template fields are NOT re-read (mutate the template via update_template between calls — old fields persist on the profile).
    - Test 3 (test_upsert_raises_no_template_error_when_no_template): Without seeding, call upsert_profile_by_vendor("nonexistent", "user-1"). Assert NoTemplateError raised; exc.vendor_type == "nonexistent"; exc message contains "nonexistent".
    - Test 4 (test_upsert_handles_integrity_error_via_reselect): Seed template. Manually pre-INSERT a profile row with the same (vendor_type, vendor_connection_id) before calling upsert. The first SELECT will find it and return; this proves the SELECT-first path works even if a row already exists (cross-process race simulator). Then specifically test the catch path: monkeypatch create_profile_from_template to raise sqlite3.IntegrityError once, then re-call — assert the function does NOT crash and returns the existing row.
    - Test 5 (test_list_profiles_filtered_returns_empty_for_no_match): Seed template + 1 profile for ("acme", "user-1"). Call list_profiles_filtered(vendor_type="other"). Assert returned list is `[]` (NOT a 404, NOT None — empty list per D-20).
    - Test 6 (test_list_profiles_filtered_returns_match_for_pair): Same seed. Call list_profiles_filtered(vendor_type="acme", vendor_connection_id="user-1"). Assert returns single-item list with the profile.
    - Test 7 (test_list_profiles_filtered_returns_all_for_no_filter): Seed 2 profiles. Call list_profiles_filtered() (no kwargs). Assert returns 2 profiles in created_at DESC order.
    - Test 8 (test_unique_constraint_enforced_at_db_layer): Direct sqlite3 INSERT of duplicate (vendor_type, vendor_connection_id) raises sqlite3.IntegrityError — proves the index from Phase 1 still exists post-init_db.
  </behavior>
  <action>

Edit `backend/database.py`. Place all new code at the end of the file (after `create_profile_from_template`). Do not modify existing helpers.

Append exactly this content to backend/database.py:

```
# ---------------------------------------------------------------------------
# Phase 2 (CONTEXT.md D-15, SESS-01): idempotent upsert by vendor pair
# ---------------------------------------------------------------------------


class NoTemplateError(Exception):
    """Raised by upsert_profile_by_vendor when no vendor_template exists for
    the given vendor_type. Surfaces as HTTP 404 in routers/sessions.py."""

    def __init__(self, vendor_type: str) -> None:
        self.vendor_type = vendor_type
        super().__init__(f"No template configured for vendor_type={vendor_type!r}")


def upsert_profile_by_vendor(
    vendor_type: str,
    vendor_connection_id: str,
) -> dict[str, Any]:
    """Idempotently get-or-create a profile for the (vendor_type, vendor_connection_id) pair.

    SELECT then (if missing) INSERT OR ABORT then re-SELECT — single-process
    serialization is provided by SessionManager._key_locks (Plan 02-04, D-02);
    the UNIQUE(vendor_type, vendor_connection_id) constraint plus IntegrityError
    swallow here is the cross-process safety net (D-15).

    Raises NoTemplateError if no vendor_template exists for vendor_type.
    """
    # Step 1 — fast path: existing row.
    with get_db() as conn:
        row = conn.execute(
            "SELECT * FROM profiles WHERE vendor_type=? AND vendor_connection_id=?",
            (vendor_type, vendor_connection_id),
        ).fetchone()
        if row:
            profile = dict(row)
            profile["launch_args"] = json.loads(profile.get("launch_args") or "[]")
            tags = conn.execute(
                "SELECT tag, color FROM profile_tags WHERE profile_id = ?",
                (profile["id"],),
            ).fetchall()
            profile["tags"] = [dict(t) for t in tags]
            return profile

        # Step 2 — template lookup. Raise on miss BEFORE attempting INSERT.
        template_row = conn.execute(
            "SELECT id, vendor_type, label, notes, blueprint, created_at, updated_at "
            "FROM vendor_templates WHERE vendor_type = ?",
            (vendor_type,),
        ).fetchone()
        if not template_row:
            raise NoTemplateError(vendor_type)
        template = dict(template_row)

    # Step 3 — snapshot via the existing helper. INSERT OR ABORT semantics:
    # IntegrityError on a concurrent cross-process race -> the other worker
    # already inserted -> we re-SELECT to return the winner's row.
    try:
        return create_profile_from_template(
            template=template,
            vendor_connection_id=vendor_connection_id,
            name=None,
        )
    except sqlite3.IntegrityError:
        # Cross-process race winner — re-SELECT to return their row.
        with get_db() as conn:
            row = conn.execute(
                "SELECT * FROM profiles WHERE vendor_type=? AND vendor_connection_id=?",
                (vendor_type, vendor_connection_id),
            ).fetchone()
            if row is None:
                # UNIQUE constraint guarantees this never happens; defensive raise.
                raise
            profile = dict(row)
            profile["launch_args"] = json.loads(profile.get("launch_args") or "[]")
            tags = conn.execute(
                "SELECT tag, color FROM profile_tags WHERE profile_id = ?",
                (profile["id"],),
            ).fetchall()
            profile["tags"] = [dict(t) for t in tags]
            return profile


def list_profiles_filtered(
    vendor_type: str | None = None,
    vendor_connection_id: str | None = None,
) -> list[dict[str, Any]]:
    """List profiles with optional (vendor_type, vendor_connection_id) filtering.

    Backs GET /profiles in routers/profiles.py (Plan 02-08, PROF-01/PROF-02).
    D-20: returns [] (not 404) on no match — list endpoint, not single-item lookup.

    - No args: all profiles.
    - vendor_type only: filter by vendor_type.
    - Both args: at most one row (UNIQUE pair).
    """
    sql = "SELECT * FROM profiles"
    where: list[str] = []
    params: list[Any] = []
    if vendor_type is not None:
        where.append("vendor_type = ?")
        params.append(vendor_type)
    if vendor_connection_id is not None:
        where.append("vendor_connection_id = ?")
        params.append(vendor_connection_id)
    if where:
        sql += " WHERE " + " AND ".join(where)
    sql += " ORDER BY created_at DESC"

    with get_db() as conn:
        rows = conn.execute(sql, tuple(params)).fetchall()
        result: list[dict[str, Any]] = []
        for row in rows:
            p = dict(row)
            p["launch_args"] = json.loads(p.get("launch_args") or "[]")
            tags = conn.execute(
                "SELECT tag, color FROM profile_tags WHERE profile_id = ?",
                (p["id"],),
            ).fetchall()
            p["tags"] = [dict(t) for t in tags]
            result.append(p)
        return result
```

DO NOT modify any existing function. DO NOT change `init_db()` — the UNIQUE INDEX is already there (line 134).
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; python -c "from backend.database import upsert_profile_by_vendor, NoTemplateError, list_profiles_filtered; assert callable(upsert_profile_by_vendor); assert issubclass(NoTemplateError, Exception); assert callable(list_profiles_filtered); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'class NoTemplateError\(Exception\):' backend/database.py` matches exactly one line
    - `grep -nE 'def upsert_profile_by_vendor' backend/database.py` matches exactly one line
    - `grep -nE 'def list_profiles_filtered' backend/database.py` matches exactly one line
    - `grep -c 'except sqlite3.IntegrityError' backend/database.py` returns at least 1
    - `grep -c 'INSERT OR IGNORE' backend/database.py` returns 0 (must NOT use OR IGNORE — Pitfall F)
    - `python -c "from backend.database import upsert_profile_by_vendor, NoTemplateError, list_profiles_filtered"` exits 0
    - The verify command prints OK
  </acceptance_criteria>
  <done>NoTemplateError, upsert_profile_by_vendor, list_profiles_filtered all defined in backend/database.py; existing functions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append unit tests to backend/tests/test_database.py</name>
  <files>backend/tests/test_database.py</files>
  <read_first>
    - backend/tests/test_database.py (current contents — what fixtures and patterns already exist)
    - backend/tests/conftest.py (tmp_db fixture at line 43-50 — points DB_PATH and DATA_DIR at tmp_path)
    - .planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-RESEARCH.md §"4. upsert_profile_by_vendor — Concrete SQL + Python"
  </read_first>
  <behavior>Same 8 tests listed in Task 1's behavior section.</behavior>
  <action>

Append the following test functions to backend/tests/test_database.py. Reuse the existing `tmp_db` fixture from conftest. If `test_database.py` already has a top of file with imports, add only the new imports needed; otherwise create a new section header.

```
# ---------------------------------------------------------------------------
# Phase 2 (CONTEXT.md D-15, SESS-01, SESS-08, PROF-01, PROF-02): upsert tests
# ---------------------------------------------------------------------------

import json as _json
import sqlite3 as _sqlite3
from unittest.mock import patch as _patch

import pytest

from backend import database as _db
from backend.database import (
    NoTemplateError,
    create_template,
    list_profiles_filtered,
    update_template,
    upsert_profile_by_vendor,
)


def _seed_template(vendor_type: str = "acme", **blueprint_overrides) -> dict:
    blueprint = {
        "timezone": "America/New_York",
        "locale": "en-US",
        "platform": "windows",
        "screen_width": 1920,
        "screen_height": 1080,
        "humanize": False,
        "human_preset": "default",
        "launch_args": [],
        "clipboard_sync": False,
    }
    blueprint.update(blueprint_overrides)
    return create_template(
        vendor_type=vendor_type,
        label=f"{vendor_type} template",
        notes=None,
        blueprint_json=_json.dumps(blueprint),
    )


def test_upsert_creates_new_profile_when_template_exists(tmp_db):
    template = _seed_template("acme", timezone="UTC")
    profile = upsert_profile_by_vendor("acme", "user-1")
    assert profile["vendor_type"] == "acme"
    assert profile["vendor_connection_id"] == "user-1"
    assert profile["template_id"] == template["id"]
    assert profile["timezone"] == "UTC"  # snapshot from template
    # Confirm exactly one row in profiles
    with _db.get_db() as conn:
        rows = conn.execute(
            "SELECT id FROM profiles WHERE vendor_type=? AND vendor_connection_id=?",
            ("acme", "user-1"),
        ).fetchall()
    assert len(rows) == 1


def test_upsert_returns_existing_profile_unchanged(tmp_db):
    template = _seed_template("acme", timezone="UTC")
    first = upsert_profile_by_vendor("acme", "user-1")
    # Mutate the template — TMPL-05: profile must NOT change on next upsert
    update_template(template["id"], blueprint_json=_json.dumps({
        "timezone": "Europe/London",
        "locale": "en-GB",
        "platform": "windows",
        "screen_width": 1920,
        "screen_height": 1080,
        "humanize": False,
        "human_preset": "default",
        "launch_args": [],
        "clipboard_sync": False,
    }))
    second = upsert_profile_by_vendor("acme", "user-1")
    assert second["id"] == first["id"]
    assert second["timezone"] == "UTC"  # unchanged — snapshot, not live read
    # Still exactly one profile row
    with _db.get_db() as conn:
        rows = conn.execute(
            "SELECT id FROM profiles WHERE vendor_type=? AND vendor_connection_id=?",
            ("acme", "user-1"),
        ).fetchall()
    assert len(rows) == 1


def test_upsert_raises_no_template_error_when_no_template(tmp_db):
    with pytest.raises(NoTemplateError) as exc_info:
        upsert_profile_by_vendor("nonexistent", "user-1")
    assert exc_info.value.vendor_type == "nonexistent"
    assert "nonexistent" in str(exc_info.value)


def test_upsert_handles_integrity_error_via_reselect(tmp_db):
    """Simulate a cross-process race: the FIRST SELECT misses (no row),
    then create_profile_from_template raises IntegrityError as if another
    worker won the race. The function must catch and re-SELECT, returning
    the row that the (simulated) winner inserted."""
    _seed_template("acme")

    # Pre-insert the row that the "other worker" would have created
    pre_existing = upsert_profile_by_vendor("acme", "user-1")

    # Now monkeypatch create_profile_from_template to ALWAYS raise IntegrityError,
    # and force a fresh code path by deleting our cache assumption: the second
    # call's first SELECT will hit the existing row and return early — that's
    # the natural fast path. To exercise the IntegrityError branch, we delete
    # the row, then patch get_template/SELECT to think it's missing, then call:
    # easier: just patch create_profile_from_template to raise, but since the
    # SELECT will now find the row, the patched function never runs.
    # Therefore: delete the row, monkeypatch create_profile_from_template to
    # raise, and re-INSERT the row underneath via direct SQL (simulating a
    # racing winner that inserted between our SELECT-miss and INSERT).

    # Delete our row, keep template
    with _db.get_db() as conn:
        conn.execute("DELETE FROM profiles WHERE id = ?", (pre_existing["id"],))
        conn.commit()

    inserted_id = {"id": None}

    def _race_insert(template, vendor_connection_id, name=None):
        """Simulate a winning concurrent worker: insert a row directly, then
        raise IntegrityError as if our INSERT lost the race."""
        # Use the same data shape as create_profile_from_template
        with _db.get_db() as conn:
            new_id = "race-winner-id"
            inserted_id["id"] = new_id
            conn.execute(
                "INSERT INTO profiles (id, name, fingerprint_seed, vendor_type, "
                "vendor_connection_id, template_id, user_data_dir, created_at, updated_at) "
                "VALUES (?, ?, ?, ?, ?, ?, ?, datetime('now'), datetime('now'))",
                (new_id, "racer", 99999, template["vendor_type"], vendor_connection_id,
                 template["id"], "/tmp/racer"),
            )
            conn.commit()
        raise _sqlite3.IntegrityError("UNIQUE constraint failed: idx_profiles_vendor_pair")

    with _patch("backend.database.create_profile_from_template", side_effect=_race_insert):
        result = upsert_profile_by_vendor("acme", "user-1")

    assert result["id"] == inserted_id["id"]
    assert result["vendor_type"] == "acme"


def test_list_profiles_filtered_returns_empty_for_no_match(tmp_db):
    _seed_template("acme")
    upsert_profile_by_vendor("acme", "user-1")
    out = list_profiles_filtered(vendor_type="other")
    assert out == []


def test_list_profiles_filtered_returns_match_for_pair(tmp_db):
    _seed_template("acme")
    profile = upsert_profile_by_vendor("acme", "user-1")
    out = list_profiles_filtered(vendor_type="acme", vendor_connection_id="user-1")
    assert len(out) == 1
    assert out[0]["id"] == profile["id"]


def test_list_profiles_filtered_returns_all_for_no_filter(tmp_db):
    _seed_template("acme")
    _seed_template("globex")
    upsert_profile_by_vendor("acme", "user-1")
    upsert_profile_by_vendor("globex", "user-2")
    out = list_profiles_filtered()
    assert len(out) == 2


def test_unique_constraint_enforced_at_db_layer(tmp_db):
    """SESS-08 verification: the UNIQUE INDEX from Phase 1 init_db rejects
    duplicate (vendor_type, vendor_connection_id) at the DB layer."""
    _seed_template("acme")
    upsert_profile_by_vendor("acme", "user-1")
    with _db.get_db() as conn:
        with pytest.raises(_sqlite3.IntegrityError):
            conn.execute(
                "INSERT INTO profiles (id, name, fingerprint_seed, vendor_type, "
                "vendor_connection_id, user_data_dir, created_at, updated_at) "
                "VALUES (?, ?, ?, ?, ?, ?, datetime('now'), datetime('now'))",
                ("dup-id", "dup", 11111, "acme", "user-1", "/tmp/dup"),
            )
            conn.commit()
```
  </action>
  <verify>
    <automated>cd /Users/troy/Code/_kickback/CloakBrowser-Manager &amp;&amp; pytest backend/tests/test_database.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_upsert_creates_new_profile_when_template_exists\|def test_upsert_returns_existing_profile_unchanged\|def test_upsert_raises_no_template_error_when_no_template\|def test_upsert_handles_integrity_error_via_reselect\|def test_list_profiles_filtered_returns_empty_for_no_match\|def test_list_profiles_filtered_returns_match_for_pair\|def test_list_profiles_filtered_returns_all_for_no_filter\|def test_unique_constraint_enforced_at_db_layer' backend/tests/test_database.py` returns 8
    - `pytest backend/tests/test_database.py -x` exits 0
    - `pytest backend/tests/test_database.py -k "upsert" -v` shows at least 4 tests passing
    - `pytest backend/tests/test_database.py -k "list_profiles_filtered" -v` shows at least 3 tests passing
  </acceptance_criteria>
  <done>All 8 new database tests pass alongside any existing tests in test_database.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent POST /sessions across processes -> upsert_profile_by_vendor | Multi-worker race window between SELECT miss and INSERT; UNIQUE constraint + IntegrityError catch ensures exactly one row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 (cross-process) | Tampering | upsert_profile_by_vendor (concurrent insert race, Pitfall 2) | mitigate | UNIQUE(vendor_type, vendor_connection_id) index from Phase 1 + try/except sqlite3.IntegrityError + re-SELECT pattern; INSERT OR ABORT default semantics, never INSERT OR IGNORE (Pitfall F) |
| T-02-10 (defense in depth) | Tampering | upsert returning stale data after template edit (TMPL-05 violation) | mitigate | upsert_profile_by_vendor SELECTs the existing profile row first and returns it unchanged; never re-reads vendor_templates if a profile row exists. Verified by test_upsert_returns_existing_profile_unchanged |
</threat_model>

<verification>
- All 8 new tests in test_database.py pass
- Existing test_database.py tests (Phase 1 coverage) still pass
- `grep -c 'INSERT OR IGNORE' backend/database.py` returns 0 (Pitfall F not violated)
- `init_db()` and the UNIQUE index creation at line 134 are unchanged
</verification>

<success_criteria>
- `pytest backend/tests/test_database.py -x` exits 0
- `python -c "from backend.database import upsert_profile_by_vendor, NoTemplateError, list_profiles_filtered"` exits 0
- D-15 invariant: upsert returns same row on second call without re-reading template (test_upsert_returns_existing_profile_unchanged)
- SESS-08 invariant: direct duplicate INSERT raises IntegrityError (test_unique_constraint_enforced_at_db_layer)
</success_criteria>

<output>
After completion, create `.planning/phases/02-sessions-warm-pool-and-cdp-lifecycle/02-03-SUMMARY.md` describing:
- Final line count of backend/database.py (existing + new)
- The exact public symbols added: NoTemplateError, upsert_profile_by_vendor, list_profiles_filtered
- Confirmation that no existing helper was modified
- Pitfall F (no INSERT OR IGNORE) explicitly verified
</output>
