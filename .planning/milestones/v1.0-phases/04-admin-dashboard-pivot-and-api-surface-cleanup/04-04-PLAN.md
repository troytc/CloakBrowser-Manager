---
phase: 04-admin-dashboard-pivot-and-api-surface-cleanup
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - backend/main.py
  - backend/tests/test_api.py
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/hooks/useProfiles.ts
  - frontend/src/hooks/useProfiles.test.ts
autonomous: true
requirements: [OPS-01, OPS-02]
tags: [backend, cleanup, deprecation, tests]

must_haves:
  truths:
    - "POST /api/profiles/{id}/launch returns 410 or 404"
    - "POST /api/profiles/{id}/stop returns 410 or 404"
    - "POST /api/profiles (create) and PUT /api/profiles/{id} (full update) and GET /api/profiles (list) are removed or return 410"
    - "Admin VNC, clipboard, CDP, DELETE /api/profiles/{id} still work"
    - "Full pytest suite passes"
  artifacts:
    - path: "backend/main.py"
      provides: "trimmed admin profile surface"
      contains: "vnc"
    - path: "frontend/src/lib/api.ts"
      provides: "no launchProfile/stopProfile/createProfile"
  key_links:
    - from: "backend/tests/test_api.py"
      to: "410 launch/stop"
      via: "assert status_code == 410"
      pattern: "launch"
---

<objective>
Remove legacy admin profile launch/stop and redundant CRUD endpoints after the Sessions UI is live; align frontend API client with the new surface.

Purpose: OPS-01 and OPS-02 — clean API contract; no dual paths for profile lifecycle.
Output: Deprecated routes gone, tests updated, dead frontend API methods removed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-CONTEXT.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-RESEARCH.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-03-SUMMARY.md
@backend/main.py
@backend/tests/test_api.py
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy backend endpoints</name>
  <files>backend/main.py, backend/tests/test_api.py</files>
  <action>
In `main.py` **delete** handlers (not redirect):
- `POST /api/profiles/{profile_id}/launch`
- `POST /api/profiles/{profile_id}/stop`
- `GET /api/profiles` (list)
- `POST /api/profiles` (create)
- `PUT /api/profiles/{profile_id}` (full update)
- `GET /api/profiles/{profile_id}` **only if** unused after 04-03 — grep frontend; if `ProfileViewer` needs no GET, remove it

**Keep:**
- `DELETE /api/profiles/{profile_id}` (ops cleanup)
- `GET /api/profiles/{profile_id}/status` — remove if redundant with admin sessions list; else keep for debugging
- VNC WS, CDP routes, clipboard routes
- Phase 3 viewer routes and security middleware — do not regress

Update `test_api.py`:
- Replace `test_launch_*` expectations with `test_launch_returns_410` (or 404 if chosen — document in test name)
- Remove tests that depend on `POST /api/profiles` create for non-VNC scenarios OR rewrite to use DB fixtures / upsert helpers
- Add `test_list_profiles_removed` asserting GET `/api/profiles` → 404/410
- Ensure VNC/clipboard/CDP tests still pass

Optional: remove unused `LaunchResponse` from imports if nothing references it.
  </action>
  <verify>cd backend && pytest tests/test_api.py tests/test_admin_sessions.py -q</verify>
  <done>Backend tests green; launch/stop return gone status</done>
</task>

<task type="auto">
  <name>Task 2: Frontend API cleanup + full test suite</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.test.ts, frontend/src/hooks/useProfiles.ts, frontend/src/hooks/useProfiles.test.ts</files>
  <action>
In `api.ts`, remove unused methods:
- `listProfiles`, `createProfile`, `updateProfile`, `launchProfile`, `stopProfile`
- Keep `deleteProfile` only if exposed in UI; otherwise remove
- Keep clipboard helpers if `ProfileViewer` still uses them

Delete `useProfiles.ts` and `useProfiles.test.ts` if nothing imports them (grep `useProfiles`).

Update `api.test.ts` — remove launchProfile tests.

Run full backend pytest from repo root (275+ tests baseline). Run `cd frontend && npm test -- --run`.

If `ProfileList.tsx`, `ProfileForm.tsx`, `LaunchButton.tsx` are orphaned with zero imports, delete those files in this task.
  </action>
  <verify>cd /Users/troy/Code/_kickback/CloakBrowser-Manager/backend && pytest -q --tb=no 2>&1 | tail -3; cd ../frontend && npm test -- --run 2>&1 | tail -5</verify>
  <done>Full pytest + frontend tests pass; no references to launch/stop in frontend/src</done>
</task>

</tasks>

<verification>
- `grep -n "launch_profile\|stop_profile" backend/main.py` — no route definitions (comments OK)
- `grep -r "launchProfile\|listProfiles" frontend/src` — no matches
- `pytest -q` full suite passes
</verification>

<success_criteria>
- OPS-01: launch/stop removed
- OPS-02: legacy admin list/create/update removed; machine `/profiles` + admin `/api/admin/sessions` remain the supported surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-04-SUMMARY.md`
</output>
