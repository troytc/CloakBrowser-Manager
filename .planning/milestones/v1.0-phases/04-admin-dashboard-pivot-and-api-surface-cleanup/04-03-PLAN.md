---
phase: 04-admin-dashboard-pivot-and-api-surface-cleanup
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/App.tsx
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04]
tags: [frontend, react, dashboard-pivot]

must_haves:
  truths:
    - "Dashboard default surface is Sessions; switcher shows Sessions and Templates only"
    - "Selecting a running session opens ProfileViewer via admin VNC WebSocket"
    - "No ProfileForm, LaunchButton, or New Profile entry points remain in App"
    - "Templates list/create/edit/delete still works"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "sessions + templates surfaces only"
      contains: "SessionList"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ProfileViewer"
      via: "view === view when selected session state running"
      pattern: "ProfileViewer"
    - from: "frontend/src/App.tsx"
      to: "SessionList"
      via: "surface === \"sessions\""
      pattern: "SessionList"
---

<objective>
Rewire the admin dashboard from legacy Profiles CRUD to Sessions ops + Templates, with admin VNC on running rows.

Purpose: Delivers ADM-01 verification, ADM-02/03/04 in the UI before legacy API removal.
Output: Pivot complete `App.tsx`; checkpoint for human smoke test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-CONTEXT.md
@.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-02-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/SessionList.tsx
@frontend/src/components/ProfileViewer.tsx
@frontend/src/hooks/useSessions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx sessions surface + remove legacy profile UI</name>
  <files>frontend/src/App.tsx</files>
  <action>
Refactor `AppContent` in `App.tsx`:

1. Replace `useProfiles()` with `useSessions()` for the ops surface.
2. Change `Surface` type to `"sessions" | "templates"`; default `surface` to `"sessions"`.
3. Sidebar: render `SessionList` instead of `ProfileList` when `surface === "sessions"`.
4. Remove imports and usage: `ProfileList`, `ProfileForm`, `LaunchButton`, `useProfiles` create/update/launch/stop handlers, `View` modes `create`/`edit` for profiles.
5. Session selection state: `selectedSessionId`; on select:
   - If session `state === "running"`: set view to `"view"` and render `ProfileViewer` with `profileId`, `clipboard_sync` from session row, `cdpUrl` null or omit
   - Else: set view to `"detail"` showing read-only summary (vendor pair, state, counts, message: “Profile stopped — wake via Main App POST /sessions”) — no launch button
6. Top bar: when session selected, show `vendor_type` / `vendor_connection_id` + `StatusIndicator`; remove `LaunchButton`.
7. Segmented control: **Sessions** | **Templates** (rename from Profiles).
8. Keep templates branch unchanged (ADM-01 regression guard).
9. Error banner: `surface === "sessions" ? sessionsError : templatesError`.

Preserve auth flow (`LoginPage`, logout) unchanged.
  </action>
  <verify>cd frontend && npx tsc --noEmit --skipLibCheck && npm run build 2>&1 | tail -8</verify>
  <done>Build succeeds; App has no ProfileForm/LaunchButton references</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test dashboard pivot</name>
  <what-built>Sessions + Templates dashboard; admin VNC for running sessions; legacy profile create/launch removed from UI</what-built>
  <how-to-verify>
1. Start backend + frontend dev server with admin auth configured.
2. Open dashboard — default tab is **Sessions**; no “New Profile” button.
3. **Templates:** create/edit/delete a template (confirm ADM-01).
4. **Sessions:** if no rows, create a profile via `curl -X POST /sessions` with `X-API-Key` (or use existing DB row); confirm row shows vendor_type, connection id, state, counts.
5. For a **running** session, select row — VNC viewer connects (admin path).
6. For a **stopped** session, confirm no launch control; informational state only.
7. Confirm browser network tab calls `GET /api/admin/sessions` (not `/api/profiles` list) on poll.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -E 'ProfileForm|LaunchButton|ProfileList|handleLaunch|handleNew' frontend/src/App.tsx` returns no matches
- `grep 'SessionList' frontend/src/App.tsx` matches
</verification>

<success_criteria>
- ADM-03: running session opens admin VNC
- ADM-04: no profile creation or launch UI
- ADM-01: templates still functional
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-pivot-and-api-surface-cleanup/04-03-SUMMARY.md`
</output>
