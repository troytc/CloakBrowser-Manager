---
id: 03-03
phase: 03-signed-viewer-urls-and-security-hardening
plan: 3
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - backend/vnc_proxy_core.py
  - backend/routers/viewer.py
  - backend/static/viewer/index.html
  - backend/static/viewer/viewer.js
  - backend/main.py
  - backend/tests/test_viewer_routes.py
requirements:
  - VIEW-04
  - VIEW-05
  - VIEW-06
  - VIEW-07
  - VIEW-08
  - VIEW-09
autonomous: true
must_haves:
  truths:
    - "GET /viewer/{profile_id} serves embed HTML with CSP frame-ancestors MAIN_APP_ORIGIN"
    - "WebSocket /viewer/{profile_id}/ws validates JWT before accept; replay/exp/wrong profile → 4401"
    - "JTI consumed on first successful WS auth before VNC proxy starts"
    - "viewer_attach_count increments on connect and decrements in finally; feeds idle timer"
    - "Admin /api/profiles/{id}/vnc still works unchanged (cookie auth)"
  artifacts:
    - path: "backend/vnc_proxy_core.py"
      provides: "Shared VNC proxy loop extracted from main.py"
    - path: "backend/routers/viewer.py"
      provides: "Viewer HTML + WS routes"
    - path: "backend/static/viewer/index.html"
      provides: "noVNC embed page per UI-SPEC"
  key_links:
    - from: "backend/routers/viewer.py"
      to: "backend/vnc_proxy_core.py"
      via: "await run_vnc_proxy(websocket, profile_id)"
      pattern: "run_vnc_proxy"
    - from: "backend/routers/viewer.py"
      to: "backend/viewer_tokens.py"
      via: "validate + consume_jti before accept"
      pattern: "consume_jti"
    - from: "backend/static/viewer/viewer.js"
      to: "/viewer/{id}/ws"
      via: "token from location.hash appended to ws URL"
      pattern: "location.hash"
---

<objective>
Deliver the viewer surface: static embed page (fragment token), viewer WebSocket with JWT + JTI enforcement, shared VNC proxy core, and viewer_attach_count idle integration. Implements VIEW-04 through VIEW-09.

Purpose: Core Phase 3 deliverable — iframe embed streams VNC securely.

Output: `backend/routers/viewer.py`, static viewer assets, refactored VNC core, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-RESEARCH.md
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-UI-SPEC.md
@frontend/src/components/ProfileViewer.tsx
@backend/main.py
@backend/session_manager.py
@backend/security_csp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract VNC proxy core and register viewer router skeleton</name>
  <files>backend/vnc_proxy_core.py, backend/routers/viewer.py, backend/main.py</files>
  <read_first>
    - backend/main.py vnc_proxy function (lines ~763-923)
    - backend/main.py _filter_rfb_client_messages and RFB helpers
    - .planning/phases/03-signed-viewer-urls-and-security-hardening/03-RESEARCH.md
  </read_first>
  <action>
    Create `backend/vnc_proxy_core.py` by moving the body of `vnc_proxy` (post-accept logic: websockets.connect, client_to_vnc, vnc_to_client, RFB filter) into `async def run_vnc_proxy(websocket: WebSocket, profile_id: str, running: RunningProfile) -> None`.

    Refactor `main.py::vnc_proxy` to: origin check → running lookup → accept → call `run_vnc_proxy` (no viewer token).

    Create `backend/routers/viewer.py` with `APIRouter(prefix="/viewer", tags=["viewer"])` — stub routes for now.

    In `main.py`, `app.include_router(viewer_router)` **before** static SPA catch-all (after existing routers).

    Import RunningProfile type from browser_manager.
  </action>
  <verify>pytest backend/tests/test_api.py -k vnc -x --timeout=60 2>/dev/null || pytest backend/tests/ -k "vnc" -x -q</verify>
  <acceptance_criteria>
    - `backend/vnc_proxy_core.py` contains `async def run_vnc_proxy`
    - `grep -c "run_vnc_proxy" backend/main.py` is >= 1
    - `backend/routers/viewer.py` exists
    - Existing admin VNC WS tests or manual smoke still pass (no behavioral change to /api/profiles/{id}/vnc)
  </acceptance_criteria>
  <done>VNC core shared; viewer router registered.</done>
</task>

<task type="auto">
  <name>Task 2: Viewer static page + noVNC hash bootstrap</name>
  <files>backend/static/viewer/index.html, backend/static/viewer/viewer.js, backend/routers/viewer.py</files>
  <read_first>
    - .planning/phases/03-signed-viewer-urls-and-security-hardening/03-UI-SPEC.md
    - frontend/src/components/ProfileViewer.tsx (RFB options)
    - frontend/package.json (@novnc/novnc version)
  </read_first>
  <action>
    Implement GET `/viewer/{profile_id}`:
    - Returns `FileResponse` or `HTMLResponse` for `backend/static/viewer/index.html`
    - Sets CSP header via `security_csp.viewer_frame_ancestors_header()`

    Static `viewer.js`:
    - Parse `#token=` from `window.location.hash`
    - Build ws URL: `${proto}//${host}/viewer/${profileId}/ws?token=${encodeURIComponent(token)}`
    - Load noVNC from CDN **or** copy minimal `@novnc/novnc/core/rfb.js` into `backend/static/viewer/vendor/` during build (prefer vendored copy in static/ for offline Docker — copy from node_modules in Dockerfile or commit vendor slice)

    Match ProfileViewer RFB settings: `scaleViewport=true`, `resizeSession=false`, `wsProtocols: ['binary']`.

    Error UI per UI-SPEC for missing token.
  </action>
  <verify>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/viewer/test-id  # expect 200 when server running; or pytest static route test</verify>
  <acceptance_criteria>
    - `backend/static/viewer/index.html` and `viewer.js` exist
    - `viewer.js` contains `location.hash` and `token` string parsing
    - GET `/viewer/{profile_id}` route registered on viewer router
    - Response includes `Content-Security-Policy` with `frame-ancestors` and value from MAIN_APP_ORIGIN env in tests
  </acceptance_criteria>
  <done>Embed page serves with fragment token bootstrap.</done>
</task>

<task type="auto">
  <name>Task 3: Viewer WebSocket auth, JTI consume, attach counts, tests</name>
  <files>backend/routers/viewer.py, backend/tests/test_viewer_routes.py</files>
  <read_first>
    - backend/viewer_tokens.py
    - backend/main.py cdp_proxy try/finally attach pattern
    - .planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md D-03, D-05
  </read_first>
  <action>
    Implement `@router.websocket("/{profile_id}/ws")`:
    1. `_check_websocket_origin` (reuse from main — import or move to shared util)
    2. Read token from query param `token` (set by viewer.js only)
    3. `viewer_tokens.validate(token, profile_id)` — any failure → close 4401 reason "Unauthorized" **before** accept
    4. `consume_jti(claims["jti"])` — if False → 4401
    5. `running = browser_mgr.running.get(profile_id)` — if missing → 4004
    6. `await websocket.accept(...)` with binary subprotocol
    7. Increment `viewer_attach_count` under lock; `sm.on_attach(profile_id)`
    8. `try: await run_vnc_proxy(...)` `finally:` decrement + on_all_detached

    Add `backend/tests/test_viewer_routes.py`:
    - Mint valid token → WS connects (mock KasmVNC or patch run_vnc_proxy to no-op)
    - Replay same token → second WS gets 4401
    - Wrong profile_id in path → 4401
    - Expired token → 4401

    Apply viewer CSP on WS response if applicable (HTTP 101 upgrade — CSP on HTML route is primary VIEW-08).
  </action>
  <verify>pytest backend/tests/test_viewer_routes.py -x</verify>
  <acceptance_criteria>
    - `test_viewer_routes.py` has at least 4 tests passing
    - `consume_jti` called in viewer WS handler before `run_vnc_proxy`
    - `viewer_attach_count` incremented in viewer WS handler (grep)
    - All failure paths use close code 4401
  </acceptance_criteria>
  <done>Viewer WS enforces JWT + JTI + idle counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /viewer/{id}/ws | JWT + JTI before VNC bytes flow |
| Main App iframe → viewer HTML | frame-ancestors restricts embedding origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Spoofing | Unauthenticated VNC access | mitigate | validate + consume_jti before accept |
| T-03-09 | Replay | URL shared twice | mitigate | JTI single-use |
| T-03-10 | Information Disclosure | Distinct WS close reasons | mitigate | Generic 4401 for all auth failures |
| T-03-11 | Tampering | CSWSH on viewer WS | mitigate | Reuse _check_websocket_origin |
</threat_model>

<verification>
pytest backend/tests/test_viewer_routes.py backend/tests/test_viewer_tokens.py -x
</verification>

<success_criteria>
- Viewer WS tests pass
- Static viewer page and WS route tests pass (VIEW-09 partial — full iframe checkpoint in Plan 03-04)
</success_criteria>

<output>
After completion, create `.planning/phases/03-signed-viewer-urls-and-security-hardening/03-03-SUMMARY.md`
</output>
