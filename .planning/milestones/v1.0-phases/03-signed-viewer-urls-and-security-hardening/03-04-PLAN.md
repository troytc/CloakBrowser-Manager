---
id: 03-04
phase: 03-signed-viewer-urls-and-security-hardening
plan: 4
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - backend/session_manager.py
  - backend/tests/test_sessions_router.py
  - backend/tests/test_warm_pool_e2e.py
requirements:
  - VIEW-01
autonomous: false
must_haves:
  truths:
    - "POST /sessions returns non-empty vnc_viewer_url"
    - "vnc_viewer_url matches /viewer/{profile_id}#token={jwt} pattern"
    - "Second POST /sessions returns new jti (different token string)"
  artifacts:
    - path: "backend/session_manager.py"
      contains: "vnc_viewer_url"
  key_links:
    - from: "backend/session_manager.py::get_or_wake"
      to: "backend/viewer_tokens.py::mint"
      via: "f\"/viewer/{profile_id}#token={token}\""
      pattern: "viewer_tokens.mint"
---

<objective>
Wire `viewer_tokens.mint()` into `SessionManager.get_or_wake()` so `POST /sessions` returns the signed fragment URL (VIEW-01). Update tests that currently assert empty `vnc_viewer_url`.

Purpose: Completes the Main App API contract for Phase 3.

Output: Populated `vnc_viewer_url` on every successful wake.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md
@backend/session_manager.py
@backend/routers/sessions.py
@backend/tests/test_sessions_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint vnc_viewer_url in SessionManager.get_or_wake</name>
  <files>backend/session_manager.py</files>
  <read_first>
    - backend/session_manager.py SessionResult and get_or_wake return paths
    - backend/viewer_tokens.py mint()
    - .planning/phases/03-signed-viewer-urls-and-security-hardening/03-CONTEXT.md D-04, D-07
  </read_first>
  <action>
    Import `viewer_tokens` in session_manager.py.

    Add helper:
    ```python
    def _build_vnc_viewer_url(profile_id: str) -> str:
        token = viewer_tokens.mint(profile_id)
        return f"/viewer/{profile_id}#token={token}"
    ```

    In **every** `SessionResult(...)` return in `get_or_wake` (running, newly launched, and any early-return paths that expose a live session), set `vnc_viewer_url=_build_vnc_viewer_url(profile_id)`.

    Do NOT put token in querystring. Path is relative (Main App resolves against service base URL).

    When profile is stopped and wake fails, leave `vnc_viewer_url=""` only if no running session — if returning running state, always mint fresh token.
  </action>
  <verify>pytest backend/tests/test_sessions_router.py -x</verify>
  <acceptance_criteria>
    - `session_manager.py` imports `viewer_tokens`
    - `session_manager.py` contains `#token=` in vnc_viewer_url construction
    - All successful `get_or_wake` returns include non-empty `vnc_viewer_url`
  </acceptance_criteria>
  <done>SessionManager mints viewer URL on wake.</done>
</task>

<task type="auto">
  <name>Task 2: Update session router tests for fragment URL</name>
  <files>backend/tests/test_sessions_router.py</files>
  <read_first>
    - backend/tests/test_sessions_router.py test_post_session_success
    - backend/tests/conftest.py VIEWER_SECRET
  </read_first>
  <action>
    Replace assertions `assert body["vnc_viewer_url"] == ""` with:
    - `assert body["vnc_viewer_url"].startswith(f"/viewer/{profile_id}#token=")`
    - Decode token from fragment; `viewer_tokens.validate(token, profile_id)` succeeds

    Add `test_post_session_returns_fresh_token_on_repeat`: two POSTs same vendor keys; extract tokens from both URLs; assert tokens differ (different jti).

    Use monkeypatched VIEWER_SECRET from conftest.

    If `test_warm_pool_e2e.py` asserts empty `vnc_viewer_url`, update expectations to non-empty fragment URL.
  </action>
  <verify>pytest backend/tests/test_sessions_router.py -x</verify>
  <acceptance_criteria>
    - `test_sessions_router.py` has zero assertions that `vnc_viewer_url == ""` for successful POST
    - Repeat POST test asserts two different JWT strings
    - No failing assertion expecting empty vnc_viewer_url in e2e file after change
  </acceptance_criteria>
  <done>VIEW-01 covered by automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full VIEW-09 path: POST /sessions returns vnc_viewer_url; iframe/browser opens viewer; VNC streams; replay blocked.</what-built>
  <how-to-verify>
    1. Start service with MAIN_APP_ORIGIN, VIEWER_SECRET, MAIN_APP_API_KEY set
    2. POST /sessions with valid API key for a template-backed profile
    3. Open `vnc_viewer_url` in browser (or embed iframe from MAIN_APP_ORIGIN)
    4. Confirm VNC desktop renders; reload same URL — WS should fail (JTI replay)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main App POST /sessions | Receives short-lived URL; must not log fragment server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | Token in server access logs | mitigate | Fragment in URL only returned in JSON body; never request viewer URL server-side with fragment |
</threat_model>

<verification>
pytest backend/tests/test_sessions_router.py backend/tests/test_viewer_tokens.py -x
</verification>

<success_criteria>
- VIEW-01 satisfied: POST /sessions returns fragment viewer URL
- pytest test_sessions_router.py passes
- Human checkpoint approved for iframe embed (VIEW-09)
</success_criteria>

<output>
After completion, create `.planning/phases/03-signed-viewer-urls-and-security-hardening/03-04-SUMMARY.md`
</output>
